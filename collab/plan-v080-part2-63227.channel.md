
## MSG-1 | 2026-08-20T19:53:29+00:00 | from: owner | type: review-request | thread: plan-v080-part2-1 | refs: feature/installation-onboarding-v080@36852335ff92d8c97db83721bfa8e99741ed1cfe

Review request: the v0.8.0 part-2 plan doc `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md` (materialized in the case docket by content — docs/plans/ is untracked by design). Round 1 of the plan gate; the doc header is `Status: DRAFT — pending debate review`.

The plan makes every approved seat debate-capable out of the box and makes a debate fast enough to wait for: a generic prompt-bridge in the engine so any `{prompt}`-style seat opens brokered without a hand-authored bridge; vendor-scoped wrapper-sibling detection (detection is never approval); curated capability classes with a mismatched-pair warning mirroring the same-weights refusal; concurrent sealed invocations; post-reveal deliberation-as-continuation scoped to one seat, one case; engine-composed fold-delta dockets; experience budgets as acceptance criteria. Owner rulings of 2026-08-20 folded: ships with the unmerged `feature/installation-onboarding-v080` branch as one 0.8.0 release (the pinned export is that branch's gate-passed head), all four slices in scope.

Verify per the docket `plan-docket.md`: (1) section-2 repo-evidence claims against the export at the pinned ref; (2) whole-document internal coherence (invariants vs contracts vs slices vs obligations); (3) safety/trust invariants — the sealed boundary, the continuation scoping, fail-closed bridging, no credential handling; (4) honesty of capability claims; (5) gate/lifecycle compliance; (6) the docket's exact pytest command. Verdicts cite your own fresh evidence from the export, never this request. Name every blocking finding establishable in your pass, not the first one.


Controller-Docket-Provenance:
- topology: recommended-three-agent
- controller-config-sha256: f2e666b4e80bd369861937dd1f5e960f98c10d9f3e3f23b9a4aea4299ed213d9
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- docket-revision-sha256: e30815e692c962872235ba799087434a902befb724e5e0c139cd919eb51eec1b
- docket-files: [{"path": "collab/PROTOCOL.md", "sha256": "dfb3950a24d38ac308632baa117e961ae9da00ed1b8aac8a48261d8cbeaeb9c6", "tracked_at_source_ref": true}, {"path": "collab/plan-v080-part2-63227.debate.json", "sha256": "24740b4f553046288ff8d69a939e8bde3959b09183bc06c34addb936440e7a49", "tracked_at_source_ref": false}, {"path": "docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md", "sha256": "f17a947e4f347922a606cd62a90fc6d55715ff16b88d88c26a99e47d464b8513", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-docket.md", "sha256": "764dcbc5315bc77f9c0ef020f1c66a5262643d8258c0c81891594b454dd603ae", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat-result.schema.json", "sha256": "0f03c5e1f0be74c320750c3c2baf5a5a14c39622e34babe8c0b30781578161b1", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat_adapter.py", "sha256": "059023723e14c448598515c21dcff9c4acbc844f711edb36667fa48b8064543c", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/watcher.json", "sha256": "f2e666b4e80bd369861937dd1f5e960f98c10d9f3e3f23b9a4aea4299ed213d9", "tracked_at_source_ref": false}]
- profile-sha256: {"codex": "c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8", "opus": "71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d"}
- sanitized-profile-manifests: {"codex": {"authentication_mode": "Codex subscription OAuth state exposed only to the local wrapper; user config and rules ignored", "author_relationship": "author-independent", "cli_version": "codex-cli 0.148.0", "command_sha256": "bc02f45ab947a2ea025ac951b33fdc4235b974411683e1505590178a869f8667", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "gpt-5.6-terra", "isolation_mode": "advisory", "party": "codex", "permission_policy": "read-only source export at filesystem layer; workspace-write sandbox limited to controller invocation output/build path", "provider": "openai", "reasoning_effort": "high", "requested_model": "gpt-5.6-terra", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}, "opus": {"authentication_mode": "subscription OAuth state exposed only to the local wrapper; safe mode and settings sources disabled", "author_relationship": "author-independent", "cli_version": "claude-code 2.1.235", "command_sha256": "2107038579b81e9434ce9eb3b5f185939951bc78b3a675d4a1e8435a55f7fa27", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "claude-opus-5", "isolation_mode": "advisory", "party": "opus", "permission_policy": "read-only source export; result path controller-owned; Read/Grep/Glob plus test-only Bash allowlist", "provider": "anthropic", "reasoning_effort": "high", "requested_model": "claude-opus-5", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}}
- source-manifest-sha256: {"codex": "bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3", "opus": "55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df"}

## MSG-2 | 2026-08-20T20:02:36+00:00 | from: owner | type: close | thread: plan-v080-part2-1 | refs: -

Controller closed the managed case as ERROR. Reason: adapter-error. Supervisor messages were not counted as party votes.

Controller-Terminal:
- terminal-result: ERROR
- close-reason: adapter-error

## MSG-3 | 2026-08-20T20:03:21+00:00 | from: owner | type: review-request | thread: plan-v080-part2-2 | refs: feature/installation-onboarding-v080@36852335ff92d8c97db83721bfa8e99741ed1cfe

Review request (re-open): case 1 on this channel (MSG-1..2) closed ERROR before any seat returned — the first seat's wrapper enforced a 540 s process timeout below the controller's 1200 s budget and was killed mid-review; no verdict was produced and nothing was folded. The adapter's internal timeout is now 1150 s; the artifact and docket are unchanged. This is still round 1: review fresh on your own evidence.

The v0.8.0 part-2 plan doc `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md` (materialized in the case docket by content — docs/plans/ is untracked by design). The doc header is `Status: DRAFT — pending debate review`.

The plan makes every approved seat debate-capable out of the box and makes a debate fast enough to wait for: a generic prompt-bridge in the engine so any `{prompt}`-style seat opens brokered without a hand-authored bridge; vendor-scoped wrapper-sibling detection (detection is never approval); curated capability classes with a mismatched-pair warning mirroring the same-weights refusal; concurrent sealed invocations; post-reveal deliberation-as-continuation scoped to one seat, one case; engine-composed fold-delta dockets; experience budgets as acceptance criteria. Owner rulings of 2026-08-20 folded: ships with the unmerged `feature/installation-onboarding-v080` branch as one 0.8.0 release (the pinned export is that branch's gate-passed head), all four slices in scope.

Verify per the docket `plan-docket.md`: (1) section-2 repo-evidence claims against the export at the pinned ref; (2) whole-document internal coherence (invariants vs contracts vs slices vs obligations); (3) safety/trust invariants — the sealed boundary, the continuation scoping, fail-closed bridging, no credential handling; (4) honesty of capability claims; (5) gate/lifecycle compliance; (6) the docket's exact pytest command. Verdicts cite your own fresh evidence from the export, never this request. Name every blocking finding establishable in your pass, not the first one.


Controller-Docket-Provenance:
- topology: recommended-three-agent
- controller-config-sha256: f2e666b4e80bd369861937dd1f5e960f98c10d9f3e3f23b9a4aea4299ed213d9
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- docket-revision-sha256: 029a9087a493fb3f95b4216b39e83b2f28523fde855c32653a37370fe4c9ad0a
- docket-files: [{"path": "collab/PROTOCOL.md", "sha256": "dfb3950a24d38ac308632baa117e961ae9da00ed1b8aac8a48261d8cbeaeb9c6", "tracked_at_source_ref": true}, {"path": "collab/plan-v080-part2-63227.debate.json", "sha256": "24740b4f553046288ff8d69a939e8bde3959b09183bc06c34addb936440e7a49", "tracked_at_source_ref": false}, {"path": "docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md", "sha256": "f17a947e4f347922a606cd62a90fc6d55715ff16b88d88c26a99e47d464b8513", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-docket.md", "sha256": "764dcbc5315bc77f9c0ef020f1c66a5262643d8258c0c81891594b454dd603ae", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat-result.schema.json", "sha256": "0f03c5e1f0be74c320750c3c2baf5a5a14c39622e34babe8c0b30781578161b1", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat_adapter.py", "sha256": "5e3f30bd1d0e3dd0b2ff605bc3b4412970912744c8456f944af6128ad46e71a6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/watcher.json", "sha256": "f2e666b4e80bd369861937dd1f5e960f98c10d9f3e3f23b9a4aea4299ed213d9", "tracked_at_source_ref": false}]
- profile-sha256: {"codex": "c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8", "opus": "71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d"}
- sanitized-profile-manifests: {"codex": {"authentication_mode": "Codex subscription OAuth state exposed only to the local wrapper; user config and rules ignored", "author_relationship": "author-independent", "cli_version": "codex-cli 0.148.0", "command_sha256": "bc02f45ab947a2ea025ac951b33fdc4235b974411683e1505590178a869f8667", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "gpt-5.6-terra", "isolation_mode": "advisory", "party": "codex", "permission_policy": "read-only source export at filesystem layer; workspace-write sandbox limited to controller invocation output/build path", "provider": "openai", "reasoning_effort": "high", "requested_model": "gpt-5.6-terra", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}, "opus": {"authentication_mode": "subscription OAuth state exposed only to the local wrapper; safe mode and settings sources disabled", "author_relationship": "author-independent", "cli_version": "claude-code 2.1.235", "command_sha256": "2107038579b81e9434ce9eb3b5f185939951bc78b3a675d4a1e8435a55f7fa27", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "claude-opus-5", "isolation_mode": "advisory", "party": "opus", "permission_policy": "read-only source export; result path controller-owned; Read/Grep/Glob plus test-only Bash allowlist", "provider": "anthropic", "reasoning_effort": "high", "requested_model": "claude-opus-5", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}}
- source-manifest-sha256: {"codex": "bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3", "opus": "55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df"}

## MSG-4 | 2026-08-20T20:14:55+00:00 | from: opus | type: verdict | thread: plan-v080-part2-2 | refs: -

## Verification run (docket criterion 6)

Command, run literally in the pinned export root `.../exports/36852335ff92d8c97db83721bfa8e99741ed1cfe/opus`:

`python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py`

Result: `111 passed in 8.80s` (72 dots then 39 dots, no failures, no warnings summary). Criterion 6 is satisfied. The verdict below rests on criteria 1-5.

## Criterion 1 — section-2 repo evidence: VERIFIED, no blockers

Every one of 2.1-2.11 says what the export says. Checked individually:

- 2.1 `controller.py:1658-1683` — `other = next(...)`, `order = (first_party, other)`, `for party in order: ... capture_sealed(...)`. Sequential, confirmed.
- 2.2 `controller.py:1107` invocation root `cases/<thread>/invocations/<sequence>-<party>-<attempt>`; `_adapter_environment` (682-706) sets per-invocation `HOME`/`XDG_*`/`TMPDIR`/`TEMP`/`TMP` and is called only at `controller.py:1138` with `invocation_root`; the shared write is `_write_case` at `1330-1331` inside `capture_sealed`.
- 2.3 `render_input` refusal at `889-891`; `session_persistence` refusal at `159-162`; default `False` at `124`.
- 2.4 `opening.py:375-381` `_brokered_adapter` refusal; `opening.py:252-257` `{prompt}` refusal; `seats.py:468-475` `add_seat`'s two shapes; all five `CATALOG` entries (`seat_catalog.py:57-112`) carry `{prompt}`.
- 2.5 `_identity_guard` at `opening.py:106-124` with exactly the three refusals; case-insensitive Grep for `capability` over `src/` returns "No matches found" — no tier comparison exists.
- 2.6 `CatalogEntry` fields at `seat_catalog.py:43-54` (no sibling/capability field); `discover` at `seats.py:261-289` iterates `CATALOG`, resolves the first binary via `which`, seeds per submodel, examines nothing else on PATH; `Seat` at `34-50` with `source`/`cost_mode`; `_seat_from_raw` at `68-111` tolerates absent optionals.
- 2.7 `_candidates` `onboarding.py:159-179` with exactly the ten named fields; `_candidate_revision` `182-186` sha256 of the rows; `_scan` `189-208` writes nothing; `approve` at `227`.
- 2.8 `_parse_result` `709-767` (schema_version 1, entry_type, non-empty body/runtime_model, `sender` forbidden, `expected_runtime_model` only when set); `_brokered_adapter` `opening.py:387-407` sets no `expected_runtime_model`, `requested_model = seat.submodel`, `timeout_seconds` 1200, `retry_limit` 1; `_published_body` `controller.py:1215-1231` prints the named provenance lines.
- 2.9 `__main__.py:578-589` registers `broker-revise`; `revise_case` `controller.py:990-1050+` snapshots a revision and refuses profile/topology/timing changes.
- 2.10 `watcher.py:1068-1120` sleeps `interval_seconds` per tick; `opening.py:512` writes `scheduler_interval_seconds: 5`; `__main__.py:201-215` `_watch_interval` = flag > config > 180.
- 2.11 `pyproject.toml:7` is `version = "0.8.0"`, `CHANGELOG.md:9` is `## v0.8.0 — unreleased`, `tests/test_release_sync.py:11-25` pins four locations. "No version bump" is coherent.

Observation only (not blockers, per the docket's own rule): a few ranges are off by a line or two while the named code is exactly where the plan says — 2.1's loop body ends at 1683 not 1680; `Seat` starts at 34 not 35; `_seat_from_raw` ends at 111; the `_brokered_adapter` refusal ends at 381.

Also verified as accurate although outside section 2: 1a's layer-1 claim (the after-table disclosure and ask-once invitation are in `skills/debate-onboarding/SKILL.md:46-60`), and 3.4's claim that environment additions are hashed in the sanitized manifest (`controller.py:256`, `environment_additions`).

## Blocking findings (criteria 2-4)

**F1. The generic bridge's config-home pointer inverts a tested controller invariant that the plan never cites.** `controller.py:38-59` defines `_RESERVED_ENV`, whose first two members are `CLAUDE_CONFIG_DIR` and `CODEX_HOME`; `AdapterProfile.__post_init__` refuses any profile that sets them in `environment` (174-183) or inherits them via `environment_allowlist` (184-193), and `tests/test_controller.py:631-643` pins both refusals under the name `test_profiles_refuse_live_user_settings_and_controller_owned_environment` (it passed in my run). Contract 3.4 has the engine-shipped bridge set exactly those two variables to `<real home>/.claude` and `<real home>/.codex`, one process below profile validation, using a real-home value that `open --brokered` captures into the profile environment as `DEBATE_BRIDGE_REAL_HOME` specifically because the sandboxed bridge cannot discover it. The design may well be the right call, but it re-draws the sandbox boundary the engine currently enforces, and the plan does not name the guard, does not argue the exception, and section 5 lists no change to it or its test. Section 2's eleven evidence items include the `session_persistence` refusal but omit the one refusal that directly governs the mechanism 3.4 introduces. As an implementation contract this is not evidence-grounded on its central new mechanism. Fix: cite `_RESERVED_ENV` and `test_controller.py:631-643`, state plainly that the bridge re-introduces the operator's config home below the sandbox, say why the profile-level refusal must remain, and add a test pinning both halves.

**F1b (same mechanism, distinct gap).** 3.4 sets `isolation_argv` to none for kimi and for wrappers while still pointing/symlinking the real config home (`<sandbox home>/.kimi-code -> <real home>/.kimi-code`). For those seats the operator's user-level configuration becomes live execution policy for a sealed seat — the class of thing `controller.py:163-167` refuses at the profile level ("project settings remain evidence, not execution policy"). Nothing in the plan detects bleed: the Slice C canary test watches the *bridge* process ("the bridge never opens any file under the fake real home ... its stdout/stderr/result never contain a canary"), and the bridge is not what reads the config home — the seat CLI is. The existing `contamination_canaries` config key is not extended to this path. I judge the pointer to honor I6's letter (no engine read/copy/print of auth material); I do not judge it complete against the docket's "safety and trust invariants complete and non-contradictory".

**F2. 3.3 and 3.4 give contradictory accounts of where the resumable session lives.** 3.3 grounds I2's cross-case isolation on directory scoping: the case-scoped sandbox `HOME` means the CLI's session store "is unreachable from any other case (different thread, different directory)". 3.4 then redirects the vendor's config home to the real home for Claude and Codex — the same two vendors 3.3 names as the continuation targets. The plan never states which directory actually holds the resumable session, so the isolation argument for I2 cannot be evaluated, and the Slice B test as written ("a second thread ... its case-scoped home is a different directory") asserts the directory rather than the store, so it would pass either way. I am not voting on where the host CLIs store sessions (host-level, per the docket) — the defect is that the document asserts a property whose mechanism its own next section redirects. Fix: state which store the resume path depends on and make the test assert that store, or drop the directory argument and rest I2 on token privacy alone.

**F3. 3.5's default sibling pattern contradicts the examples it names.** "default derived as `<vendor>-*-agent` for entries whose catalogued binary matches that form (`codex-agent`, `glm-agent`, `deepseek-flash-agent`)". Under glob semantics `codex-*-agent` requires `codex-` + something + `-agent`, i.e. it matches `codex--agent`, not `codex-agent`; likewise `glm-agent`. Only `deepseek-flash-agent` matches. Read literally, two of the three enumerated catalog entries derive no pattern and the feature silently covers a third of what the contract says it covers. One-line fix, but it is in the section the plan designates as law.

**F4. B5 has no test, while section 7 says the seats verify it from tests.** I8 makes B1-B5 acceptance criteria and 7.3 says "budgets B3-B5 verified by the seats from tests". Section 4's test lists cover B3 (`test_latency_budget.py`) and the per-invocation half of B5 ("exactly one seat invocation per run", Slice C), but nothing asserts B5's case-level budget — two model calls for a symmetric agreement, four for a one-lap disagreement. O2 cites B5 and discharges only the per-invocation half. With fake seats this is trivially testable, so the gap is an omission, not an impossibility.

**F5. I7/B4 is not discharged where the forbidden vocabulary demonstrably lives.** `skills/debate-onboarding/SKILL.md` contains 11 occurrences of bridge/brokered/managed-version wording, of which at least two are quoted *user-facing* sentences: line 52-56 ("for a fully managed debate each one first needs a small adapter (a 'bridge')") and line 87-93 ("it can't join a fully managed debate until it gets a bridge adapter"). Slice C's word-list test is scoped to "every `print`/hint string touched by this slice", which excludes skill markdown, and Slice C step 8 enumerates the skill edits as sibling rows, declaration routing and mismatch explanation only. Since Slice C's whole point is that those sentences stop being true, the plan should name their removal and put the skill files inside the word-list test's scope.

## Non-blocking observations

- Structure: per the docket's instruction I judge the one-document shape (contract in 3, mini-plans in 4) explicitly, and I do **not** find it a blocking defect — the sections are separable, each obligation names a slice, and every criterion above was evaluable as written.
- 3.4's parenthetical "(For Claude with `--output-format json --json-schema`, the structured envelope is parsed instead)" describes argv the contract never adds; the catalogued `isolation_argv` for Claude is the four safety flags. As specified, that parse path is unreachable.
- `_brokered_adapter` sets `environment_allowlist: ["PATH", "LANG", "LC_ALL"]` (`opening.py:402`); the plan does not say whether a generic-bridge seat needs cert/proxy variables for a real vendor CLI, which its Slice C field acceptance depends on.
- 3.6 says `--yes` accepts item 1 and also that `--yes` never confirms a mismatch. When the fallback "existing default" is a mismatched `last_pair`, precedence is unstated; the intended reading (the mismatch gate still applies after selection) should be one explicit sentence.
- Contract items with no obligation number: 3.2's byte-identical sealed `render_input` golden and 3.4's inline-limit refusal.

## Checks that came out clean

- B3 is executable as written: `drive_case` advances docket → sealed → both captures → `reveal_pair` inside one tick (`controller.py:1646-1683`), `reveal_pair` closes terminal on agreement (`1593-1601`), and `watch` tests `until_close` before sleeping (`watcher.py:1113-1120`) — a zero-latency fake-seat case terminates in the first tick at the 5 s default, so the budget does not require an interval override (I9 respected).
- Slice order C, A, B, D matches the stated dependencies (A's proportional pairing consumes C's capability classes; B's golden test is stated against Slice A's output; D's cache-order test consumes C's prompt order).
- Honesty items hold: runtime model published as declared via `runtime_model_basis` with a provenance line; cache-friendliness claimed as cost with the field pass explicitly not measuring speed (3.7, O15); the 60-70 percent deliberation figure is marked expected; nothing proposed is described as shipped, and the layer-1 claim in 1a checks out against the skill text.
- Criterion 5 holds: `collab/plan-v080-part2-63227.debate.json` is parties opus/codex, supervisor owner, managed_version 2, thread_cap 12 — consistent with 7.1's fresh brokered channel; 7.3's `--refs feature/installation-onboarding-v080@<sha>` read after the last commit matches PROTOCOL.md §2 ("Refs are full `branch@sha` values written after the commit exists"); corrections-as-new-entries and human-only merges/publication (PROTOCOL.md §4) match 7.4-7.5.
- Zero model calls in discovery, detection-never-approves, no `/tmp` anywhere in the design, and `session_persistence` staying refused are consistent across I1-I9, 3.2-3.7 and section 4.

## Verdict

NO_PASS. The named pytest command passes and all eleven section-2 evidence claims verify, but F1/F1b (the config-home mechanism contradicts and does not cite `_RESERVED_ENV` and its passing test, with no contamination coverage for the seats that get no isolation flags), F2 (3.3 vs 3.4 on the session store, with a test that cannot detect the difference), F3 (3.5's pattern versus its own examples), F4 (B5 declared acceptance with no test) and F5 (I7/B4 unenforced on the surfaces that carry the forbidden words today) each need a documented fix before this becomes the implementation contract. This list is complete to my pass: I re-walked criteria 1-5 item by item and have no further blockers to add.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: ea2aae83ee85ce14948ea7bd2e9e1c2526ad02f5a43da96ab4d6ab112adce771
- phase: sealed
- captured-at: 2026-08-20T20:11:07+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: 71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d
- controller-config-sha256: f2e666b4e80bd369861937dd1f5e960f98c10d9f3e3f23b9a4aea4299ed213d9
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: 55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df
- docket-revision-sha256: 029a9087a493fb3f95b4216b39e83b2f28523fde855c32653a37370fe4c9ad0a
- input-sha256: d2a9992cc30f8a10a39c74b478f82c0c887a077f292409ff05575b10445282b0
- requested-model: claude-opus-5
- runtime-model: claude-opus-5
- reasoning-effort: high
- cli-version: claude-code 2.1.235
- isolation-mode: advisory

## MSG-5 | 2026-08-20T20:14:55+00:00 | from: codex | type: verdict | thread: plan-v080-part2-2 | refs: -

The required verification passed: `python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py` → `111 passed in 9.10s`.

Blocking findings:
1. Section 2.2 is contradicted. It says the case file is the only shared resource between sealed captures, but `capture_sealed` also calls `channel.update_managed_phase` (controller.py:1332-1341), which takes the shared channel lock and atomically writes the shared signal (channel.py:701-736). The plan must correct the evidence and specify serialization/ownership of this write in concurrent mode.
2. Section 2.10 overstates the watch behavior: `watch` calls `_run_once_locked` per tick (watcher.py:1107-1120), while `drive_case` is reached only through conditional recovery/deadline/invocation branches (watcher.py:918-995), not on every tick.
3. Section 2.11 says the release-sync test pins four locations. `tests/test_release_sync.py:12-25` checks five version values: pyproject, package `__version__`, Claude plugin, Claude marketplace metadata, and Codex plugin.
4. Slice D is not executable as written: it requires verbatim R2 and R3 clauses, but neither the pinned export nor immutable docket defines those clauses or a canonical source. Command `rg -n --hidden -g '!**/.git/**' 'R0|R2|R3|gate-acceleration|delta-round|fold-list' . "$DKT"` found only references in this plan (apart from an unrelated setup comment). The required generated docket and its test oracle are therefore undefined.
5. The sibling contract has no stable sibling identifier. `SiblingCandidate` is defined only as vendor/binary_name/binary_path and the prescribed sibling candidate row omits `seat_id` (plan §3.5), yet the contract requires `approve --allow <sibling-id>` to reject that identifier. Define and expose a deterministic, collision-safe identifier before approval/refusal behavior can be implemented or tested.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: ea2aae83ee85ce14948ea7bd2e9e1c2526ad02f5a43da96ab4d6ab112adce771
- phase: sealed
- captured-at: 2026-08-20T20:14:55+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8
- controller-config-sha256: f2e666b4e80bd369861937dd1f5e960f98c10d9f3e3f23b9a4aea4299ed213d9
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3
- docket-revision-sha256: 029a9087a493fb3f95b4216b39e83b2f28523fde855c32653a37370fe4c9ad0a
- input-sha256: 13469ea2c7ab1d56c63a0818e4d90ca505fd3d38b71a2d57bec778f014591362
- requested-model: gpt-5.6-terra
- runtime-model: gpt-5.6-terra
- reasoning-effort: high
- cli-version: codex-cli 0.148.0
- isolation-mode: advisory

## MSG-6 | 2026-08-20T20:14:55+00:00 | from: owner | type: close | thread: plan-v080-part2-2 | refs: -

Controller closed the managed case as NO_PASS. Reason: party-vote-agreement. Supervisor messages were not counted as party votes.

Controller-Terminal:
- terminal-result: NO_PASS
- close-reason: party-vote-agreement

## MSG-7 | 2026-08-20T20:23:59+00:00 | from: owner | type: review-request | thread: plan-v080-part2-3 | refs: feature/installation-onboarding-v080@36852335ff92d8c97db83721bfa8e99741ed1cfe

Fix-report and round-2 review request (fold-delta) for the v0.8.0 part-2 plan doc `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md`. Round 1 (case `plan-v080-part2-2`) closed NO_PASS at MSG-6 on MSG-4 (opus: F1, F1b, F2, F3, F4, F5 + observations) and MSG-5 (codex: 1-5). Every finding has been folded:

1. Evidence corrected: 2.2 names the shared signal write (`update_managed_phase`) and 3.2 serializes it with the case-state write outside the worker threads; 2.10 describes the `_run_once_locked` tick path; 2.11 names the five version locations; new 2.12 cites `_RESERVED_ENV`, its refusals, and its test.
2. Generic bridge: the sandbox boundary is stated and argued (guard unchanged at the profile level; the pointer applied one process below, for the seat CLI only), declared in `adapter-doctor` and a new `configuration-home` provenance line, and covered by the existing canary scan through seat-output forwarding; cert/proxy allowlist; one parser for every vendor.
3. Continuation: the case-scoped home is removed; 3.3 names the session store (the vendor's, under the operator's pointed config dir) and rests I2 on controller-enforced token privacy; `--session-id` minting for Claude, `--json` thread id for Codex (hedged, fresh fallback); the no-persistence flag dropped only for continuation seats.
4. Siblings: glob `<vendor>*-agent` with catalogued names excluded; deterministic id `vendor/wrapper:<binary>`; `add_seat` refuses `:` in a submodel.
5. B5 gains a case-level test (2 / 4 calls); `--yes` precedence stated; the skill markdown enters the plain-words test scope and the two user-facing "bridge" sentences are named for removal; R2/R3 clause texts reproduced verbatim in section 9 as the Slice D oracle; obligations O17-O19 added.

The round-1 artifact version is materialized in the docket as `plan-round1.md`; compute the true change set yourself per the docket's fold-delta clause. Verdicts cite your own fresh evidence; standing round-1 criteria may be cited by MSG id where your diff and sweep leave them unimplicated.


Controller-Docket-Provenance:
- topology: recommended-three-agent
- controller-config-sha256: 1dc7a43f0a1007287cd2bd01f55f4ded7f7dd5e49a30a997774e2247daf14f35
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- docket-revision-sha256: df5c39dbf0615c6ef154a0631cdc91d0725a5c7eeb6032eeaaf10e0ade82e855
- docket-files: [{"path": "collab/PROTOCOL.md", "sha256": "dfb3950a24d38ac308632baa117e961ae9da00ed1b8aac8a48261d8cbeaeb9c6", "tracked_at_source_ref": true}, {"path": "collab/plan-v080-part2-63227.debate.json", "sha256": "24740b4f553046288ff8d69a939e8bde3959b09183bc06c34addb936440e7a49", "tracked_at_source_ref": false}, {"path": "docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md", "sha256": "933830bf70887ff52e7e359c2d35b9e361d0063e031eaa5b2679fe4d2256453b", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-round1.md", "sha256": "f17a947e4f347922a606cd62a90fc6d55715ff16b88d88c26a99e47d464b8513", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-docket-r2.md", "sha256": "33dd134816c19f730cbe7defa5e2cd7241f486ae8ba23fee0f547e3fb2c76082", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat-result.schema.json", "sha256": "0f03c5e1f0be74c320750c3c2baf5a5a14c39622e34babe8c0b30781578161b1", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat_adapter.py", "sha256": "5e3f30bd1d0e3dd0b2ff605bc3b4412970912744c8456f944af6128ad46e71a6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/watcher-r2.json", "sha256": "1dc7a43f0a1007287cd2bd01f55f4ded7f7dd5e49a30a997774e2247daf14f35", "tracked_at_source_ref": false}]
- profile-sha256: {"codex": "c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8", "opus": "71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d"}
- sanitized-profile-manifests: {"codex": {"authentication_mode": "Codex subscription OAuth state exposed only to the local wrapper; user config and rules ignored", "author_relationship": "author-independent", "cli_version": "codex-cli 0.148.0", "command_sha256": "bc02f45ab947a2ea025ac951b33fdc4235b974411683e1505590178a869f8667", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "gpt-5.6-terra", "isolation_mode": "advisory", "party": "codex", "permission_policy": "read-only source export at filesystem layer; workspace-write sandbox limited to controller invocation output/build path", "provider": "openai", "reasoning_effort": "high", "requested_model": "gpt-5.6-terra", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}, "opus": {"authentication_mode": "subscription OAuth state exposed only to the local wrapper; safe mode and settings sources disabled", "author_relationship": "author-independent", "cli_version": "claude-code 2.1.235", "command_sha256": "2107038579b81e9434ce9eb3b5f185939951bc78b3a675d4a1e8435a55f7fa27", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "claude-opus-5", "isolation_mode": "advisory", "party": "opus", "permission_policy": "read-only source export; result path controller-owned; Read/Grep/Glob plus test-only Bash allowlist", "provider": "anthropic", "reasoning_effort": "high", "requested_model": "claude-opus-5", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}}
- source-manifest-sha256: {"codex": "bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3", "opus": "55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df"}

## MSG-8 | 2026-08-20T20:34:26+00:00 | from: opus | type: verdict | thread: plan-v080-part2-3 | refs: -

## Verification run (criterion 6)

Run literally from the export root `/home/zoltan/Projects/debate/var/debate/plan-v080-part2-63227/controller/exports/36852335ff92d8c97db83721bfa8e99741ed1cfe/opus`:

`python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py`

Result: `111 passed in 9.82s`. Criterion 6 is satisfied; it is not the reason for this verdict.

## True change set

Computed by comparing `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md` against the prior version materialized in this docket (`var/debate/plan-v080-part2-63227/plan-round1.md`), not from the author's list. Note on scope: the docket's copy of the channel record (`collab/plan-v080-part2-63227.debate.json`) carries only channel config (parties, supervisor, thread_cap, name, project, managed_version) and NO message bodies, so the round‑1 finding texts were unavailable to me; I matched folds against the finding IDs the docket itself enumerates.

Reverse check (b): every enumerated round‑1 finding has a corresponding fold present in the true change set — MSG‑5 #1 (2.2/3.2 rewritten), #2 (2.10), #3 (2.11), #4 (2.9 + new section 9 + 3.7/Slice D), #5 (3.5 sibling id); MSG‑4 F1 (new 2.12 + 3.4 "Sandbox boundary"), F1b (doctor/provenance line + canary forwarding), F2 (case‑scoped home removed from 3.3/Slice B step 4/Files), F3 (`<vendor>*-agent`), F4 (B5 both halves), F5 (SKILL.md:52‑56, 87‑93 named), observations (Claude envelope path removed, cert/proxy allowlist, `--yes` precedence, O17‑O19). No enumerated finding is fold‑less.

## Blocking findings

**1. Criterion 3 — I2's token‑privacy argument is NOT complete without the directory claim; as folded it contradicts I1 for same‑vendor pairs.**

Fold 8 (MSG‑4 F2) removes the case‑scoped home and rests continuation isolation entirely on: "only the controller holds the token, and it releases it to one (party, thread) in the deliberation phase only; a process that does not receive the token cannot resume (it does not know the id)" (3.3). That argument covers *resumption* only. It does not cover *reading*. Chain of plan text:

- 3.4 points every bridge seat at a per‑VENDOR, not per‑party, directory: `CLAUDE_CONFIG_DIR -> <real home>/.claude`, `CODEX_HOME -> <real home>/.codex`.
- 3.3 drops `--no-session-persistence` / `--ephemeral` for a continuation seat, so party A's sealed session is written into that shared store for the case's duration.
- `_identity_guard` (verified fresh at `src/debate/opening.py:106-124`) refuses same `seat_id`, identical selected argv, and same `(vendor, submodel)` — it does NOT refuse two seats of the same vendor with different submodels. 3.6's own curated classes make such a pair the *expected* symmetric frontier pick (claude opus + claude sonnet, both "frontier").
- 3.2 defaults `sealed_concurrency` to `"concurrent"`, so both sealed seats run at the same time against that one store.
- 3.4's isolation flags (Claude `--safe-mode --setting-sources "" --strict-mcp-config --disable-slash-commands`; Codex `--ignore-user-config --ignore-rules`) restrict configuration, not filesystem reads, and 3.4 states plainly that seats keep "operator-level configuration live".

Consequence: for a same‑vendor pair, party B's seat process is pointed at the very directory holding party A's persisted sealed session and can enumerate/read it without ever knowing a token. That is a flow between the two sealed invocations, which I1 declares "absolute" ("No information flows between the two sealed invocations"). The round‑1 text had a directory‑based answer (`cases/<thread>/continuation/<party>/home`, plus the test "a second thread ... its case‑scoped home is a different directory"); the fold deleted both the mechanism and that test and replaced them with a token‑only argument. 3.3 names the sharing with the operator's interactive use but never names the seat‑to‑seat case, and no Slice B test in section 4 covers it (the governance list is token‑scoped: token in payloads, token in published body/channel, token across threads).

This is the exact question the docket put to me, and the honest answer on the folded text is no. Resolving it needs either a stated restriction (no continuation for same‑vendor pairs / per‑party config home for continuation seats), or an explicit, argued, tested statement that a same‑vendor opponent's sealed session is readable — with I1 amended to match, since as written the two cannot both stand.

**2. Criterion 1 — the folded 2.2 (and 3.2 restating it) overstates when the shared signal is written.**

2.2 says `channel.update_managed_phase` is written "at the end of every `capture_sealed`", and 3.2 says "the signal is written once per recorded submission, in deterministic party order". Fresh evidence at the cited lines contradicts the frequency: `src/debate/controller.py:1332-1341` guards the call with `missing = [seat for seat in self.config.profiles if seat not in submissions]` / `if missing:`, so in a two‑party case the signal is written after the FIRST recorded submission and not after the second (missing is empty); and `controller.py:1300-1301` returns early when a submission already exists, so a re‑entered `capture_sealed` never reaches the call at all. The rest of the fold is correct and verified: the invocation root at `controller.py:1107`, the sandbox derivation at `_adapter_environment` `controller.py:682-706`, the case‑state write at `controller.py:1330-1331`, and `channel.update_managed_phase` at `src/debate/channel.py:701-736` taking `exclusive(root, name)` at 720 and writing atomically at 736. The design consequence is conservative rather than dangerous (serializing a write that happens at most once is safe), but under section 2's own rule this is a contradicted claim, not a range that is off by a line, and 2.2 is one of the claims the docket requires me to establish on fresh evidence.

**3. R3 clause — one artifact edit in the true change set is absent from the fold list.**

The section‑2 preamble was rewritten from "Every claim below names its line in the pinned export; a contradicted claim is a blocking finding." to "...names its file and line range... a contradicted claim is a blocking finding (a range off by a line or two while the named code is where the plan says is an observation)." Fold 13 covers "2.x line ranges tightened" and the second `Amended:` line; it does not name this new carve‑out, which changes the standard by which the artifact's own evidence is judged rather than any range. Per the R3 clause reproduced in this docket (and verbatim in the plan's own section 9), an artifact edit absent from the fold list is itself an unresolved finding. It should be named in the fold list or dropped.

## Checks that came out clean (fresh evidence)

- 2.12 (new, established myself): `_RESERVED_ENV` at `controller.py:38-59` names `CLAUDE_CONFIG_DIR`/`CODEX_HOME` first, then HOME/XDG_*/TMPDIR/TEMP/TMP/GIT_*/PYTEST_ADDOPTS (the set actually closes at 61 with `PYTHONDONTWRITEBYTECODE` — a two‑line undershoot, observation only); refusals at `174-193`; `settings_sources` refusal at `163-167`; `test_controller.py:631-643` pins the settings‑sources, allowlist‑inherit and environment‑override refusals; `ISOLATION_MODES` at `controller.py:35`; env additions hashed at `controller.py:256`. The 3.4 allowlist `PATH, LANG, LC_ALL, SSL_CERT_FILE, SSL_CERT_DIR, HTTP_PROXY, HTTPS_PROXY, ALL_PROXY, NO_PROXY` is correctly described as "none reserved" against that set.
- 2.9 (corrected, established myself): `broker-revise` registered at `__main__.py:578-589`; `revise_case` at `controller.py:990-1050` refusing profile/topology/timing changes. The untrackedness claim holds: `docs/plans/` is gitignored at `.gitignore:47` and the export's `docs/` contains only `assets/` and `case-study.md`, so section 9 is a genuinely self‑contained oracle. I compared section 9's `R2_CLAUSE`/`R3_CLAUSE` against this docket's own R2/R3 paragraphs — they match. Fold 4 resolves MSG‑5 #4 properly.
- 2.10 (corrected): `watcher.py:1107-1120` calls `_run_once_locked` once per tick (1108) and sleeps at 1120; `drive_case` is reached through the recovery (924), deadline (974) and invocation (989) branches inside `918-995`; `opening.py:512` writes `scheduler_interval_seconds: 5`; `__main__.py:201-215` makes it the loop default.
- 2.11 (corrected): `tests/test_release_sync.py:11-25` pins five values (pyproject, `debate.__version__`, `.claude-plugin/plugin.json`, marketplace metadata, `.codex-plugin/plugin.json`) in a function still named `test_all_four_version_locations_agree`; `pyproject.toml:7` is `0.8.0`, `CHANGELOG.md:9` is `## v0.8.0 — unreleased`.
- F3 fold: `<vendor>*-agent` matches `codex-agent`, `glm-agent`, `deepseek-flash-agent` and is `None` for `claude`/`kimi`, checked against `seat_catalog.py:57-112`; excluding the catalogued names is correct.
- F5 fold: `skills/debate-onboarding/SKILL.md:52-56` is the "(c)" sentence naming a "'bridge'" and `87-93` is the step‑3 sentence plus its "Engine fact" parenthetical — both pointers are accurate.
- F1b fold: the controller's contamination scan at `controller.py:1160-1182` runs over `proc.stdout + proc.stderr` (+ result file), so forwarding the seat's raw output into the bridge's own output does place the seat inside it; `rejection.json` and `contamination-canary-observed` are as described, and the plan is honest that `open --brokered` keeps `contamination_canaries: {}` (`opening.py:516`) so coverage depends on the operator planting one.
- Coherence: the reversal of the round‑1 bridge test ("its stdout/stderr never contain a canary") into the new controller‑level canary‑coverage test is consistent with fold 7 rather than contradictory. The sibling id `vendor/wrapper:<name>` cannot collide with catalog/manual ids given the new `:` refusal, and the deliberate consequence — the displayed candidate id can never itself be the registered id, the user declares their own — is consistent with 3.5's "the DECLARATION is the user's".
- Criterion 1 for 2.1 and 2.3‑2.8, criterion 5 (gate/lifecycle), the structure note, and the honesty items MSG‑4 listed as clean are unimplicated by the true change set or my sweep and stand by citation to MSG‑4 / MSG‑5; I additionally re‑confirmed 2.1 (`controller.py:1658-1683`), 2.3 (`889-891`, `159-162`, `124`), 2.4 (`opening.py:375-381`, `252-257`, `seats.py:468-475`), 2.5 (`opening.py:106-124`; `capability` grep over `src/` returns 0 matches across 0 files), 2.6 (`seat_catalog.py:43-54`, `seats.py:261-289`, `34-50`, `68-111`), 2.7 (`onboarding.py:159-179`, `182-186`, `189-208`, `227`) and 2.8 (`controller.py:709-767`, `opening.py:387-407` incl. the allowlist at 402, `controller.py:1215-1231`) on fresh evidence, all confirmed.
- Per the docket I did not vote on the owner's field observations, the host CLI flags at the end of section 2, or the expected latency figure.

## Verdict

NO_PASS on findings 1‑3. Finding 1 alone is disqualifying: it is a safety invariant the folds left incomplete, in the exact place the docket asked to be judged. Findings 2 and 3 are cheap text fixes.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: 30fa3588e42d6b0e186e0ad2e7a077921d117614f8fe4188966927ce7d9b144e
- phase: sealed
- captured-at: 2026-08-20T20:31:12+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: 71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d
- controller-config-sha256: 1dc7a43f0a1007287cd2bd01f55f4ded7f7dd5e49a30a997774e2247daf14f35
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: 55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df
- docket-revision-sha256: df5c39dbf0615c6ef154a0631cdc91d0725a5c7eeb6032eeaaf10e0ade82e855
- input-sha256: 54dfb07e0460ecc63cf7523b5e444cbfbeea37fbedc03f534a9a53a0a4821264
- requested-model: claude-opus-5
- runtime-model: claude-opus-5
- reasoning-effort: high
- cli-version: claude-code 2.1.235
- isolation-mode: advisory

## MSG-9 | 2026-08-20T20:34:26+00:00 | from: codex | type: verdict | thread: plan-v080-part2-3 | refs: -

Verification command: `python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py` → `111 passed in 10.88s`.

Blocking findings:
1. Section 2.2 falsely says `channel.update_managed_phase` runs “at the end of every `capture_sealed`” (plan lines 115-120). Fresh inspection of `src/debate/controller.py:1332-1341` shows it runs only when `missing` is non-empty; the second sealed capture does not update that signal. This contradicts a required repo-evidence claim and undermines the stated shared-write accounting.
2. Section 2.12 falsely says `tests/test_controller.py:631-643` “pins all” reserved environment names (plan line 198). The fresh source range tests only `CODEX_HOME`, `GIT_DIR`, and `GIT_CONFIG_KEY_0` (plus settings sources); it does not pin `CLAUDE_CONFIG_DIR`, HOME/XDG/TMP variables, `PYTEST_ADDOPTS`, or `PYTHONDONTWRITEBYTECODE` listed in `src/debate/controller.py:38-60`.
3. The continuation and bridge design conflicts with the docketed protocol safety boundary. `collab/PROTOCOL.md:117-120` requires settings sources, hooks, plugins, MCPs, and session persistence to be off. Plan 3.3 explicitly drops `--no-session-persistence`/`--ephemeral` and persists sessions in the operator’s real config store (lines 296-309); plan 3.4 then exposes real operator configuration to the seat and expressly permits kimi/wrapper seats to run with that configuration live and no isolation flags (lines 348-377). Declaration and canary scanning do not make those facilities off, so criteria 3 and 5 do not hold.

The test command passes, but these established evidence, coherence, and protocol-compliance failures bar PASS.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: 30fa3588e42d6b0e186e0ad2e7a077921d117614f8fe4188966927ce7d9b144e
- phase: sealed
- captured-at: 2026-08-20T20:34:26+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8
- controller-config-sha256: 1dc7a43f0a1007287cd2bd01f55f4ded7f7dd5e49a30a997774e2247daf14f35
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3
- docket-revision-sha256: df5c39dbf0615c6ef154a0631cdc91d0725a5c7eeb6032eeaaf10e0ade82e855
- input-sha256: 9135f40124b9b33079fa15062d6f7480c91122112bd0e9d7da35d56f7c7c725f
- requested-model: gpt-5.6-terra
- runtime-model: gpt-5.6-terra
- reasoning-effort: high
- cli-version: codex-cli 0.148.0
- isolation-mode: advisory

## MSG-10 | 2026-08-20T20:34:26+00:00 | from: owner | type: close | thread: plan-v080-part2-3 | refs: -

Controller closed the managed case as NO_PASS. Reason: party-vote-agreement. Supervisor messages were not counted as party votes.

Controller-Terminal:
- terminal-result: NO_PASS
- close-reason: party-vote-agreement

## MSG-11 | 2026-08-20T20:41:05+00:00 | from: owner | type: review-request | thread: plan-v080-part2-4 | refs: feature/installation-onboarding-v080@36852335ff92d8c97db83721bfa8e99741ed1cfe

Fix-report and round-3 review request (fold-delta) for the v0.8.0 part-2 plan doc `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md`. Round 2 (case `plan-v080-part2-3`) closed NO_PASS at MSG-10 on MSG-8 (opus 1-3 + observation) and MSG-9 (codex 1-3). Every finding has been folded; the docket restates each finding because the channel record is outside the seats' docket:

1. PROTOCOL.md section 5 outranks the session-resume design: Slice B is redesigned as verdict-carrying deliberation — no seat session persisted or resumed anywhere, no token, every process fresh; the bridge's deliberation prompt omits the docket and carries the two published verdicts; an honest `deliberation-input` provenance line; no percentage claimed.
2. Same-vendor store sharing: I1 now forbids pointing a seat at a store where an opponent's case state could persist; same-vendor pairs are admitted only under the vendor's catalogued no-persistence flag (present on every invocation), flag-less same-vendor pairs refused outright.
3. Live configuration: new I10 and evidence 2.13 cite the protocol clause; flag-less vendors (kimi, wrappers) are refused by `open --brokered` in plain words unless `--allow-live-configuration`, recorded by the doctor and an `isolation-flags` provenance value.
4. Evidence: 2.2/3.2 state the once-per-case signal write and the early return; 2.12 describes exactly what the test pins; `_RESERVED_ENV` cited as 38-61; the preamble carve-out is named in the round-1 Amended line.

The round-2 artifact version is materialized in the docket as `plan-round2.md`; compute the true change set yourself per the docket's fold-delta clause. Verdicts cite your own fresh evidence; standing round-2 criteria may be cited by MSG id where your diff and sweep leave them unimplicated.


Controller-Docket-Provenance:
- topology: recommended-three-agent
- controller-config-sha256: 587c5344b3925fdd5c72c79d6ed192decdc11185e9a47dab47541f6bba7ccd22
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- docket-revision-sha256: 20e649a523e0c2bdd2804ef2b2b74a9bff232dc23e1d07bc583997973d8cadec
- docket-files: [{"path": "collab/PROTOCOL.md", "sha256": "dfb3950a24d38ac308632baa117e961ae9da00ed1b8aac8a48261d8cbeaeb9c6", "tracked_at_source_ref": true}, {"path": "collab/plan-v080-part2-63227.debate.json", "sha256": "24740b4f553046288ff8d69a939e8bde3959b09183bc06c34addb936440e7a49", "tracked_at_source_ref": false}, {"path": "docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md", "sha256": "c9a26697c52069dbe4221387d73f64093b454dd849731b93173c2ffe20b77df3", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-round2.md", "sha256": "933830bf70887ff52e7e359c2d35b9e361d0063e031eaa5b2679fe4d2256453b", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-docket-r3.md", "sha256": "3b9c245b26b617d16d152824c1cb4a25e1328283ba344243b1815f55af5fcbdd", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat-result.schema.json", "sha256": "0f03c5e1f0be74c320750c3c2baf5a5a14c39622e34babe8c0b30781578161b1", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat_adapter.py", "sha256": "5e3f30bd1d0e3dd0b2ff605bc3b4412970912744c8456f944af6128ad46e71a6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/watcher-r3.json", "sha256": "587c5344b3925fdd5c72c79d6ed192decdc11185e9a47dab47541f6bba7ccd22", "tracked_at_source_ref": false}]
- profile-sha256: {"codex": "c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8", "opus": "71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d"}
- sanitized-profile-manifests: {"codex": {"authentication_mode": "Codex subscription OAuth state exposed only to the local wrapper; user config and rules ignored", "author_relationship": "author-independent", "cli_version": "codex-cli 0.148.0", "command_sha256": "bc02f45ab947a2ea025ac951b33fdc4235b974411683e1505590178a869f8667", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "gpt-5.6-terra", "isolation_mode": "advisory", "party": "codex", "permission_policy": "read-only source export at filesystem layer; workspace-write sandbox limited to controller invocation output/build path", "provider": "openai", "reasoning_effort": "high", "requested_model": "gpt-5.6-terra", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}, "opus": {"authentication_mode": "subscription OAuth state exposed only to the local wrapper; safe mode and settings sources disabled", "author_relationship": "author-independent", "cli_version": "claude-code 2.1.235", "command_sha256": "2107038579b81e9434ce9eb3b5f185939951bc78b3a675d4a1e8435a55f7fa27", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "claude-opus-5", "isolation_mode": "advisory", "party": "opus", "permission_policy": "read-only source export; result path controller-owned; Read/Grep/Glob plus test-only Bash allowlist", "provider": "anthropic", "reasoning_effort": "high", "requested_model": "claude-opus-5", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}}
- source-manifest-sha256: {"codex": "bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3", "opus": "55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df"}

## MSG-12 | 2026-08-20T20:49:15+00:00 | from: opus | type: verdict | thread: plan-v080-part2-4 | refs: -

## Verification run

Docket criterion 6, run literally from the export root `.../exports/36852335ff92d8c97db83721bfa8e99741ed1cfe/opus`:

`python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py`
→ `111 passed in 9.28s`. Criterion 6 PASSES.

Method note (honest limitation): `diff -u` on the two docket-materialized plan versions was denied by this seat's sandbox permission mode (two attempts, both refused). I therefore computed the true change set by reading both docket files in full — `var/debate/plan-v080-part2-63227/plan-round2.md` (839 lines) and `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md` (902 lines) — and comparing them section by section. All evidence below is my own fresh reading of the pinned export.

## Section-2 evidence the docket required fresh (criterion 1): all confirmed

- **2.2** — `controller.py:1107` invocation root ✓; `_adapter_environment` at `682-706` ✓; `_write_case` at `1330-1331` ✓; the `if missing:` guard + `update_managed_phase` at `1332-1341` ✓; early return for an already-recorded party at `1300-1301` ✓; `channel.py:701-736` with `exclusive()` at 720 and `_atomic_write` at 736 ✓. The "exactly once per two-party case, after the first recorded submission" claim is correct: after the second submission `missing` is empty and no signal is written.
- **2.3** — `889-891` ✓, `159-162` ✓, `124` ✓, and the new `controller.py:929-930` ✓ (`payload["current_thread"] = transcript or []` for `open`/`deliberation`).
- **2.5** — `opening.py:106-124` ✓; line 117 keys on `(vendor, submodel)`, so same-vendor/different-submodel is indeed admitted.
- **2.12** — `_RESERVED_ENV` at `38-61` with `PYTHONDONTWRITEBYTECODE` at 60 ✓; `174-193` ✓; `163-167` ✓; `35` ✓. `tests/test_controller.py:631-643` pins exactly the four refusals now claimed (settings sources, `CODEX_HOME` allowlist inheritance, `GIT_DIR`, `GIT_CONFIG_KEY_0`) and does not reach `CLAUDE_CONFIG_DIR` ✓.
- **2.13** — `collab/PROTOCOL.md:117-121` ✓ (line 119 is verbatim "Settings sources, hooks, plugins, MCPs and session persistence are off."); the advisory-isolation paragraph is at `123-126` ✓.
- Bookkeeping cites: `.gitignore:47` = `docs/plans/` ✓; `opening.py:516` = `"contamination_canaries": {}` ✓.

## Round-2 findings: reverse check

MSG-8 #2 / MSG-9 #1 (fold 4) ✓ resolved and independently verified. MSG-9 #2 (fold 5) ✓ resolved. MSG-8 #3 (fold 6) ✓ resolved. MSG-8 observation (fold 7) ✓ resolved. MSG-8 #1 (folds 1, 2) ✓ resolved: tokens are gone entirely, and the same-vendor read path is closed — 3.4 admits a same-vendor pair only under a catalogued `no_persistence_argv` and refuses a flag-less same-vendor pair *even under* `--allow-live-configuration`. **MSG-9 #3 is only half-resolved** — see finding 1.

## Blocking findings

**1. The `--allow-live-configuration` override contradicts three absolute statements the plan makes in the same document (criteria 2, 3, 4).** 3.4 admits a flag-less vendor (kimi, glm, deepseek, every wrapper — none has a catalogued `no_persistence_argv`) to run "with its operator-level configuration live". For such a vendor there is by definition no flag that turns session persistence off, so its CLI may persist the sealed prompt and the seat's verdict — case state — into the operator's store. Against that, the artifact asserts without carve-out:
- I2: "Session persistence stays OFF for every seat (2.13)";
- §6 non-goals: "No persisted or resumed seat session for ANY seat";
- O12: "no session persisted anywhere";
- §5 files: "No change to ... `collab/PROTOCOL.md`".

I10 is the only place that scopes the guarantee honestly ("Protocol §5 holds for every bridge seat **by default**"), and 2.13 likewise. So the plan states both "always" and "unless the operator says otherwise". Worse, 3.8's own gate ruling establishes the governing rule — "a session-resume variant would need a protocol amendment gated on its own — out of this plan's scope" — and then 3.4 ships a supported path to exactly the state §5 forbids (settings sources, hooks, plugins, MCPs and session persistence live) with no amendment. The plan cannot treat §5 as outranking design in 3.3/3.8 and as operator-waivable in 3.4/I10. Resolve by one of: (a) carve the override out of I2/§6/O12 explicitly and say plainly what a flag-less seat may persist, plus route the §5 deviation through its own gated amendment as 3.8 requires; or (b) drop the override and refuse flag-less vendors outright. Note that Slice B's test "grep the record and the runtime root — no session file, no token" cannot detect this, since a flag-less vendor writes under the operator's real home, not the runtime root.

**2. An artifact edit absent from the fold list (R3 clause; same class as MSG-8 #3).** The round-1 `Amended:` header lost the clause "continuation no longer rests on a case-scoped home — I2 rests on controller-enforced token privacy, the session store is named (3.3)" (present at round-2 lines 25-27, gone from current lines 20-32). Fold 6 names only the *addition* of the preamble carve-out to that same header line; folds 1 and 8 name I2, 1d, Slice B, sections 5/6, O12/O13, the third header line and the host-level note — not this deletion. Deleting a prior round's amendment record is not cosmetic: the round-1 header now no longer says what round-1 folding actually did. Either restore the clause (marked as superseded by round 2) or name the deletion in the fold list.

**3. The plan's own headline capability claim now overstates the design (criterion 4).** Fold 3 corrected the user-facing copy — Slice C step 9 now reads "Claude and Codex seats can debate with no extra setup; other tools need an explicit allowance or a custom seat command" — but left the plan's own claims untouched: 1b "a generic prompt-bridge shipped in the engine that wraps **ANY** prompt-style seat into a controller-bound one"; Scope "**every approved seat** debate-capable out of the box"; §4 "C is the headline ('every seat debate-capable')". Under the new admission rule only Claude and Codex qualify by default. This bites the motivating case: 1a's problem is a *deepseek* wrapper, and the Slice C acceptance now reads "a deepseek wrapper is refused in plain words and runs under `--allow-live-configuration`". O1 was corrected; these three were not.

## Observations (non-blocking)

- Fold-list item 3 cites the protocol clause as `collab/PROTOCOL.md:117-121`, `121-124`; the artifact's 2.13 correctly cites `117-121` and `123-126`. The artifact is right, the fold list is off.
- 3.4's engine surface carries `[--isolation-argv-json <json>]` but no counterpart for `no_persistence_argv`, while the Invocation bullet requires both lists on every call. Slice C step 1 has the bridge read both tables from the catalog, so this is a surface asymmetry rather than a gap.
- Two further unlisted edits, both consequential bookkeeping I would not block on: §7 "invariants I1-I9" → "I1-I10 ... consistent with PROTOCOL.md §5 (2.13)", and the "(F1b)" tag dropped from the Slice C canary-coverage test line.

## Standing by citation (MSG-8, this seat's round-2 verdict)

2.1, 2.3's first three citations, 2.4, 2.6, 2.7, 2.8, 2.10, 2.11; the F3/F5/F1b folds; the sibling-id coherence of 3.5; criterion 5's gate/lifecycle items (§7's lifecycle steps 1-5 are unchanged apart from the invariant-range wording noted above). Nothing in the true change set or the coherence sweep implicates them.

PASS requires the command to pass, every round-2 finding to be resolved, and criteria 1-5 to hold. The command passes and criterion 1 holds on fresh evidence, but criteria 2, 3 and 4 do not, and MSG-9 #3 is not fully resolved.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: b72c42b406cff50a1ebd7c4eb7a254486e9b6fa4bad923acfc7f9428966bdf53
- phase: sealed
- captured-at: 2026-08-20T20:46:56+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: 71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d
- controller-config-sha256: 587c5344b3925fdd5c72c79d6ed192decdc11185e9a47dab47541f6bba7ccd22
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: 55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df
- docket-revision-sha256: 20e649a523e0c2bdd2804ef2b2b74a9bff232dc23e1d07bc583997973d8cadec
- input-sha256: b6e0584f5cb4737b02bd565c2b78b32f361adc5471af46291b39baf46d9582ff
- requested-model: claude-opus-5
- runtime-model: claude-opus-5
- reasoning-effort: high
- cli-version: claude-code 2.1.235
- isolation-mode: advisory

## MSG-13 | 2026-08-20T20:49:15+00:00 | from: codex | type: verdict | thread: plan-v080-part2-4 | refs: -

Blocker: the round-2 Protocol §5 finding is not resolved. The immutable protocol says without exception, “Settings sources, hooks, plugins, MCPs and session persistence are off” (PROTOCOL.md:117-121). Current plan 2.13:239-241 silently adds an operator-allowance exception, and I10/3.4:292-297, 371-378 and Slice C tests:636-641 explicitly admit a flag-less vendor under `--allow-live-configuration`, with its operator-level configuration live. Recording the exception in doctor/provenance does not satisfy the protocol; no protocol amendment is proposed. This also contradicts the plan’s claim that every section-3 design is held to §5, so MSG-9 #3 remains unresolved.

Fresh verification run exactly: `python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py` -> exit 0, `111 passed in 10.21s`. The test pass cannot cure the explicit design/protocol conflict.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: b72c42b406cff50a1ebd7c4eb7a254486e9b6fa4bad923acfc7f9428966bdf53
- phase: sealed
- captured-at: 2026-08-20T20:49:15+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8
- controller-config-sha256: 587c5344b3925fdd5c72c79d6ed192decdc11185e9a47dab47541f6bba7ccd22
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3
- docket-revision-sha256: 20e649a523e0c2bdd2804ef2b2b74a9bff232dc23e1d07bc583997973d8cadec
- input-sha256: d5d3a91f2d1fbfe078258b027b639c7a35bc8d9e823188dcfaf5177b7cb60214
- requested-model: gpt-5.6-terra
- runtime-model: gpt-5.6-terra
- reasoning-effort: high
- cli-version: codex-cli 0.148.0
- isolation-mode: advisory

## MSG-14 | 2026-08-20T20:49:15+00:00 | from: owner | type: close | thread: plan-v080-part2-4 | refs: -

Controller closed the managed case as NO_PASS. Reason: party-vote-agreement. Supervisor messages were not counted as party votes.

Controller-Terminal:
- terminal-result: NO_PASS
- close-reason: party-vote-agreement

## MSG-15 | 2026-08-20T20:52:20+00:00 | from: owner | type: review-request | thread: plan-v080-part2-5 | refs: feature/installation-onboarding-v080@36852335ff92d8c97db83721bfa8e99741ed1cfe

Fix-report and round-4 review request (fold-delta) for the v0.8.0 part-2 plan doc `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md`. Round 3 (case `plan-v080-part2-4`) closed NO_PASS at MSG-14 on MSG-12 (opus 1-3 + observations) and MSG-13 (codex). Every finding has been folded; the docket restates each finding because the channel record is outside the seats' docket:

1. The `--allow-live-configuration` override is removed entirely: PROTOCOL.md section 5 admits no operator waiver. The generic bridge runs a seat only with KNOWN isolation and no-persistence flags — catalogued for Claude and Codex, or declared by the operator on a manual seat (`seats add --isolation-argv --no-persistence-argv`, the cost-mode declaration model); everything else is refused in plain words naming both paths; the bridge itself refuses to invoke without a no-persistence flag. I2, section 6 and O12 now hold without carve-out; I10 and 2.13 say "without exception"; the kimi symlink path is gone.
2. The round-1 Amended line's deleted clause is restored as superseded; the round-2 line's override sentence is marked removed.
3. Title, Scope, 1b and the section-4 headline now state exactly what the design delivers.
4. Engine surface gains `--no-persistence-argv-json`; the two previously unlisted bookkeeping edits are acknowledged in the fourth Amended line.

The round-3 artifact version is materialized in the docket as `plan-round3.md`; compute the true change set yourself per the docket's fold-delta clause. Verdicts cite your own fresh evidence; standing round-3 criteria may be cited by MSG id where your diff and sweep leave them unimplicated.


Controller-Docket-Provenance:
- topology: recommended-three-agent
- controller-config-sha256: f8059feba47fc19706949ec3882a4eea5ced973bdf66a468d42518717097429a
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- docket-revision-sha256: 379b76ff3b2debaeea9a4dce2815a568e4c7161dedbd28565b38d6de22ce8b1c
- docket-files: [{"path": "collab/PROTOCOL.md", "sha256": "dfb3950a24d38ac308632baa117e961ae9da00ed1b8aac8a48261d8cbeaeb9c6", "tracked_at_source_ref": true}, {"path": "collab/plan-v080-part2-63227.debate.json", "sha256": "24740b4f553046288ff8d69a939e8bde3959b09183bc06c34addb936440e7a49", "tracked_at_source_ref": false}, {"path": "docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md", "sha256": "bb35ecb2c965acf2caaa3a4f9420e76bf50449c9854fab899736f90e17579e67", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-round3.md", "sha256": "c9a26697c52069dbe4221387d73f64093b454dd849731b93173c2ffe20b77df3", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-docket-r4.md", "sha256": "f1cccdf10bfbe7822f5fa8ecf58c4a3de6a808aa86a86eb3d53287237afded26", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat-result.schema.json", "sha256": "0f03c5e1f0be74c320750c3c2baf5a5a14c39622e34babe8c0b30781578161b1", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat_adapter.py", "sha256": "5e3f30bd1d0e3dd0b2ff605bc3b4412970912744c8456f944af6128ad46e71a6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/watcher-r4.json", "sha256": "f8059feba47fc19706949ec3882a4eea5ced973bdf66a468d42518717097429a", "tracked_at_source_ref": false}]
- profile-sha256: {"codex": "c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8", "opus": "71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d"}
- sanitized-profile-manifests: {"codex": {"authentication_mode": "Codex subscription OAuth state exposed only to the local wrapper; user config and rules ignored", "author_relationship": "author-independent", "cli_version": "codex-cli 0.148.0", "command_sha256": "bc02f45ab947a2ea025ac951b33fdc4235b974411683e1505590178a869f8667", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "gpt-5.6-terra", "isolation_mode": "advisory", "party": "codex", "permission_policy": "read-only source export at filesystem layer; workspace-write sandbox limited to controller invocation output/build path", "provider": "openai", "reasoning_effort": "high", "requested_model": "gpt-5.6-terra", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}, "opus": {"authentication_mode": "subscription OAuth state exposed only to the local wrapper; safe mode and settings sources disabled", "author_relationship": "author-independent", "cli_version": "claude-code 2.1.235", "command_sha256": "2107038579b81e9434ce9eb3b5f185939951bc78b3a675d4a1e8435a55f7fa27", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "claude-opus-5", "isolation_mode": "advisory", "party": "opus", "permission_policy": "read-only source export; result path controller-owned; Read/Grep/Glob plus test-only Bash allowlist", "provider": "anthropic", "reasoning_effort": "high", "requested_model": "claude-opus-5", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}}
- source-manifest-sha256: {"codex": "bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3", "opus": "55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df"}

## MSG-16 | 2026-08-20T20:59:37+00:00 | from: opus | type: verdict | thread: plan-v080-part2-5 | refs: -

NO_PASS — the pytest command passes and the round-3 folds are all present, but the coherence sweep finds two blocking defects created by this round's change set: the artifact's own admission rule contains a second, non-declarable condition (a catalogued config-home variable) that the rescoped headline claims, O1, the refusal wording and the Slice C acceptance all contradict.

## Verification run

Command (run literally in the pinned export root /home/zoltan/Projects/debate/var/debate/plan-v080-part2-63227/controller/exports/36852335ff92d8c97db83721bfa8e99741ed1cfe/opus, no prefixes, suffixes, pipes or diagnostics):

`python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py`

Result: `111 passed in 10.57s` (72 dots then 39 dots, 100%). Criterion 6 satisfied.

Tooling note, as the docket permits: my sandbox refused `diff` (Bash permission denied on the `diff -u ...` invocation), so I computed the true change set by reading both versions in full — `var/debate/plan-v080-part2-63227/plan-round3.md` (902 lines) and `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md` (944 lines) — and comparing section by section, as the round-3 opus seat did. I also verified 2.13's citations directly against the docket's `collab/PROTOCOL.md` (lines 117-121 carry "Settings sources, hooks, plugins, MCPs and session persistence are off."; 123-126 carry the `isolation_mode: advisory` paragraph — both cites correct), and checked the channel config `watcher-r4.json` (fresh brokered channel, opus/codex, `session_persistence: false`, `isolation_mode: advisory`, docket_files as listed) against §7.

## True change set (mine, not the author's list)

Changed regions, round-3 → current: title (L1); round-1 `Amended:` line regains the continuation clause marked "[superseded in round 2]" (L24-27); round-2 `Amended:` line gains "[override REMOVED in round 3]" (L48); new fourth `Amended:` line (L53-67); Scope (L80-83); 1b (L118-122); 2.13 final sentences (L263-266); I1 (L286-288); I10 (L318-324); 3.4 engine surface, both argv lists now required (L378-383); 3.4 "Known flags or refusal" bullet (L393-416); 3.4 same-vendor bullet (L417-423); 3.4 sandbox-boundary bullet, kimi symlink removed (L432-449); 3.5 declaration path (L516); 3.6 precedence (L544-546); 3.8 new round-3 ruling bullet (L599-600); §4 order sentence (L606-607); Slice C steps 2, 5, 9; Slice C bridge-test bullet (kimi symlink dropped, L667); Slice C open/admission test bullet (L674-682); Slice C acceptance (L711-719); §5 files (L836-837); §6 (L864); O1 (L899); O19 (L917).

Sections I read as byte-unchanged: 2.1-2.12 and the section-2 preamble and host-level note, I2-I9, 3.2, 3.3, 3.4 prompt-construction/parsing/honesty bullets, 3.5 (except L516), 3.6 (except L544-546), 3.7, Slices A/B/D in full, §7, O2-O18 except O1/O19, §9.

Against the author's fold list: every fold (1-5) is in the true change set, and I found no artifact edit absent from the fold list. Fold 4's two bookkeeping items are correctly handled as acknowledgements only — §7 still reads "invariants I1-I10 ... consistent with PROTOCOL.md §5 (2.13)" and the canary-test bullet still lacks the "(F1b)" tag in both versions, i.e. nothing changed there this round.

Reverse check — every round-3 finding has a fold: MSG-12 #1 + MSG-13 → fold 1 (grep over the artifact for `allow-live-configuration|operator-allowed|live configuration|symlink|kimi-code` returns only L47, L54 and L60, all inside the amendment-history header, correctly preserved as superseded records); MSG-12 #2 → fold 2 (L24-27, L48); MSG-12 #3 → fold 3 (L1, L80-83, L118-122, L606-607); MSG-12 observations → fold 4 (`--no-persistence-argv-json` at L380, both lists now non-optional; the 2.13 cites read `117-121`/`123-126` and are correct; the two bookkeeping edits acknowledged at L65-67). No round-3 finding is without a fold.

## Blocking findings

**F1 (criteria 2 and 4). The admission rule has a second condition that no headline claim, no refusal message and no obligation acknowledges, and that the operator cannot satisfy.** L432-436 (rewritten this round, where the kimi symlink used to be) states: the bridge re-introduces the vendor's configuration home "through the vendor's documented variable (`CLAUDE_CONFIG_DIR` -> ...; `CODEX_HOME` -> ...; a vendor whose CLI has no such documented variable **is not served by the generic bridge** — a custom seat command remains its path)". `config_home` is a CATALOG table only, "verified strings only" (Slice C step 4, L628-630), and `seats add` gains only `--capability-class --isolation-argv --no-persistence-argv` (L633-635) — there is no declaration path for a config home. So declaring isolation and no-persistence flags makes a tool debate-capable only if its vendor ALREADY has a catalogued config-home variable, i.e. in practice only Claude and Codex, which already have catalogued flags and never needed the declaration path. This contradicts, on the artifact's own text:
- Scope, L80-83: "any other tool debate-capable once its operator declares how it turns its settings, hooks, plugins, MCPs and session saving off" — false for any tool without a catalogued config-home variable;
- 1b, L118-122: "declared by the operator for any other tool"; "A tool whose flags are unknown is not run by the bridge" states the flags condition as the ONLY exclusion;
- O1, L899: "**Any** `{prompt}` seat whose isolation + no-persistence flags are known (catalogued or declared) opens brokered without a hand-authored bridge; **all others** refused in plain words" — the config-home exclusion is a third category the obligation denies exists;
- the refusal contract itself, L406-411: `open --brokered` refuses "a generic-bridge seat that has neither a catalogued nor a declared pair of lists", with a plain-words message naming only the two flag paths — a seat with declared flags and no config-home variable falls through this rule with no defined behaviour (admitted and then run with no reachable auth, or refused with a message the plan does not specify);
- Slice C acceptance, L711-719: expects a deepseek wrapper to run "after its flags are declared, with `isolation-flags: declared` in the record"; deepseek has no catalogued config home (Slice C step 4 lists only verified strings, and 3.4 catalogues config homes only for Claude and Codex), so under L435 that wrapper is not served regardless of what the owner declares. The acceptance hedges on argument forwarding, not on this.

This is the same class of finding as MSG-12 #3 (headline overstates the design after the admission rule), only partially resolved: the claims were rescoped to the flags condition while the change set simultaneously introduced a second condition. Both sides of the contradiction are inside the true change set, so it is squarely in this round's sweep.

**F2 (criterion 2). The Invocation bullet still sources both argv lists from the vendor catalogue, excluding the declared path this round created.** L473-478: "the seat argv with `{prompt}` substituted ... plus **the vendor's catalogued** `isolation_argv` and `no_persistence_argv`". A declared seat has no vendor catalogue entry for either list — its lists live in the new optional `Seat` fields (L400-404). This contradicts L412-416 ("Both lists are appended to EVERY bridge invocation, sealed and deliberation alike (a test asserts it)"), the doctor/provenance value `declared` (L411-412, L445-449), and I10's "catalogued (verified strings) or declared by the operator on a manual seat" (L320-322). As written, the operative construction rule for the invocation silently drops the declared case; the tests bullet at L666-667 also pins the flags "for claude/codex" only, so nothing would catch it.

## Observations (not blocking, but worth folding)

1. The belt-and-braces refusal is asymmetric: the bridge "exits 2 without calling the seat if launched with an empty `--no-persistence-argv-json`" (L414-416), with no counterpart for an empty isolation list, while the admission rule speaks of "a declared **pair** of lists" (L406-408) without saying whether an empty declared list counts as declared. One clause would settle it.
2. I1 (L286-288) now makes an absolute claim — no seat is ever pointed at a store where an opponent's case state could persist — that for declared seats rests entirely on an operator assertion. 3.4 is honest about this ("the operator asserts what their tool does, the record shows it was declared, never verified", L402-404) and I4 hedges its own claim; I1 does not. A "(declared, never verified, for operator-declared seats)" clause would align it with I4's standard.

## Criteria status

- Criterion 6 (command): PASS, evidence above.
- Criterion 1 (section-2 evidence): no section-2 claim changed except 2.13's closing sentences; I re-established 2.13's PROTOCOL.md cites myself (correct). 2.1-2.12 stand by citation to MSG-12 (which cited MSG-8 where applicable) — unimplicated by the true change set.
- Criterion 2 (whole-document coherence): FAILS on F1 and F2. The absolute statements themselves are clean — I checked I1 (L281-288), I2 (L289-294), I10 (L318-324), 2.13 (L263-266), §6 (L861-865) and O12 (L910): no override flag, no operator waiver, no carve-out anywhere; the only override mentions in the file are the preserved amendment records.
- Criterion 3 (safety/§5 with no exception path): the override removal does resolve MSG-12 #1 and MSG-13 — §5 now has no waiver path in this design. I do not block on this criterion; F1 leaves the admission surface incoherent rather than reopening a §5 exception.
- Criterion 4 (honesty of capability claims): FAILS on F1 — the Scope and 1b claims and O1 do not match the artifact's own admission rule once L435 is counted.
- Criterion 5 (gate/lifecycle): §7 unchanged this round and unimplicated by the sweep; consistent with `watcher-r4.json` as read above; stands by citation to MSG-12.

To close: give the config-home condition the same declaration treatment as the flags (or state plainly in Scope, 1b, O1 and the refusal message that the declared path is limited to vendors with a catalogued configuration-home variable, and rewrite the Slice C acceptance accordingly), and fix L473-478 to source both lists from "catalogued or declared" with a test that covers a declared seat's invocation.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: 591c6658f6b53292d193ae4994d84630036314c96fe3cc710ef2588de498de33
- phase: sealed
- captured-at: 2026-08-20T20:56:56+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: 71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d
- controller-config-sha256: f8059feba47fc19706949ec3882a4eea5ced973bdf66a468d42518717097429a
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: 55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df
- docket-revision-sha256: 379b76ff3b2debaeea9a4dce2815a568e4c7161dedbd28565b38d6de22ce8b1c
- input-sha256: 8e11a9e9c3a83b24f8cb3606f8d7be90c2f884185e7e1e138ca201b3a599a42b
- requested-model: claude-opus-5
- runtime-model: claude-opus-5
- reasoning-effort: high
- cli-version: claude-code 2.1.235
- isolation-mode: advisory

## MSG-17 | 2026-08-20T20:59:37+00:00 | from: codex | type: verdict | thread: plan-v080-part2-5 | refs: -

Blocking findings:
1. The round-3 honesty fold remains incomplete. The plan says any other tool/a `{prompt}` seat with declared isolation and no-persistence flags is bridge-capable (Scope lines 81-82; §1b lines 115-123; O1 line 899), but §3.4 lines 431-436 excludes any CLI without a documented configuration-home variable. Manual seats gain no `config_home` declaration path. Thus a manual tool can satisfy the advertised flag rule yet still require a custom adapter, contradicting the revised headline claim and O1.
2. The declared-flags safety path is not specified end-to-end. §3.4 admits manual declared flags (lines 399-416), but its invocation rule appends only the vendor's catalogued flags (lines 472-476); Slice C likewise sources the tables from the catalog (lines 614-616). It also requires refusal only for an empty no-persistence list (lines 413-416, 674-680), not an empty isolation list. This leaves no defined enforcement that a manual admitted seat actually receives non-empty isolation flags, so I10's and §2.13's §5-without-exception claim does not hold.

I computed the true change set with `diff -u /home/zoltan/Projects/debate/var/debate/plan-v080-part2-63227/controller/dockets/379b76ff3b2debaeea9a4dce2815a568e4c7161dedbd28565b38d6de22ce8b1c/files/var/debate/plan-v080-part2-63227/plan-round3.md /home/zoltan/Projects/debate/var/debate/plan-v080-part2-63227/controller/dockets/379b76ff3b2debaeea9a4dce2815a568e4c7161dedbd28565b38d6de22ce8b1c/files/docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md`; it showed the stated round-3 folds and no additional artifact edit. The source manifest check reported `source_ref=36852335ff92d8c97db83721bfa8e99741ed1cfe`, `manifest_files=78`, `hash_mismatches=0`. The exact required command `python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py` returned `111 passed in 8.97s`. Unimplicated section-2/lifecycle items stand by MSG-12, but these coherence and §5 failures block PASS.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: 591c6658f6b53292d193ae4994d84630036314c96fe3cc710ef2588de498de33
- phase: sealed
- captured-at: 2026-08-20T20:59:37+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8
- controller-config-sha256: f8059feba47fc19706949ec3882a4eea5ced973bdf66a468d42518717097429a
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3
- docket-revision-sha256: 379b76ff3b2debaeea9a4dce2815a568e4c7161dedbd28565b38d6de22ce8b1c
- input-sha256: b6be3f0c9aed49e4da94f61976011c51f02dab7e850e0e92dc8a74bd3963484e
- requested-model: gpt-5.6-terra
- runtime-model: gpt-5.6-terra
- reasoning-effort: high
- cli-version: codex-cli 0.148.0
- isolation-mode: advisory

## MSG-18 | 2026-08-20T20:59:37+00:00 | from: owner | type: close | thread: plan-v080-part2-5 | refs: -

Controller closed the managed case as NO_PASS. Reason: party-vote-agreement. Supervisor messages were not counted as party votes.

Controller-Terminal:
- terminal-result: NO_PASS
- close-reason: party-vote-agreement

## MSG-19 | 2026-08-20T21:01:41+00:00 | from: owner | type: review-request | thread: plan-v080-part2-6 | refs: feature/installation-onboarding-v080@36852335ff92d8c97db83721bfa8e99741ed1cfe

Fix-report and round-5 review request (fold-delta) for the v0.8.0 part-2 plan doc `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md`. Round 4 (case `plan-v080-part2-5`) closed NO_PASS at MSG-18 on MSG-16 (opus F1, F2, two observations) and MSG-17 (codex 1-2). Every finding has been folded; the docket restates each finding because the channel record is outside the seats' docket:

1. The configuration-home pointer is no longer an admission condition: it is optional and declarable (`seats add --config-home VAR=<dir>`; catalogued for Claude and Codex); a seat without one runs fully sandboxed and must authenticate on its own (the env-pinned wrappers already do); admission is the flags rule alone, stated identically in 3.4, Scope, 1b, O1, the refusal wording and the Slice C acceptance.
2. The declared path is specified end to end: the invocation rule and Slice C step 1 source both argv lists from the catalog or the seat's declared fields; a declared pair means two non-empty lists; the bridge exits 2 if either list is empty; tests cover a declared seat's invocation, the either-empty refusal, and the pointer present/absent cases.
3. I1 hedges operator-declared seats the way I4 hedges a declared model.

The round-4 artifact version is materialized in the docket as `plan-round4.md`; compute the true change set yourself per the docket's fold-delta clause. Verdicts cite your own fresh evidence; standing round-4 criteria may be cited by MSG id where your diff and sweep leave them unimplicated.


Controller-Docket-Provenance:
- topology: recommended-three-agent
- controller-config-sha256: d2b5119174a1b97a64a15e1fd67f980ba89b03f1af0399a5aecc890073c7c9bb
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- docket-revision-sha256: 75cc1efb12cffe216448882a40e3f962075425a2a888372dc6afcb6feffa98e1
- docket-files: [{"path": "collab/PROTOCOL.md", "sha256": "dfb3950a24d38ac308632baa117e961ae9da00ed1b8aac8a48261d8cbeaeb9c6", "tracked_at_source_ref": true}, {"path": "collab/plan-v080-part2-63227.debate.json", "sha256": "24740b4f553046288ff8d69a939e8bde3959b09183bc06c34addb936440e7a49", "tracked_at_source_ref": false}, {"path": "docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md", "sha256": "2aec7cf7ab6d1023aa6f05ef7b4d281eaae951843f6f7a6ed4aacd0cebed89d6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-round4.md", "sha256": "bb35ecb2c965acf2caaa3a4f9420e76bf50449c9854fab899736f90e17579e67", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-docket-r5.md", "sha256": "81f6e798676bc8b72dba0828955f8abbf5f7fa3af00e9ba6730b278ca8d97f77", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat-result.schema.json", "sha256": "0f03c5e1f0be74c320750c3c2baf5a5a14c39622e34babe8c0b30781578161b1", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat_adapter.py", "sha256": "5e3f30bd1d0e3dd0b2ff605bc3b4412970912744c8456f944af6128ad46e71a6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/watcher-r5.json", "sha256": "d2b5119174a1b97a64a15e1fd67f980ba89b03f1af0399a5aecc890073c7c9bb", "tracked_at_source_ref": false}]
- profile-sha256: {"codex": "c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8", "opus": "71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d"}
- sanitized-profile-manifests: {"codex": {"authentication_mode": "Codex subscription OAuth state exposed only to the local wrapper; user config and rules ignored", "author_relationship": "author-independent", "cli_version": "codex-cli 0.148.0", "command_sha256": "bc02f45ab947a2ea025ac951b33fdc4235b974411683e1505590178a869f8667", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "gpt-5.6-terra", "isolation_mode": "advisory", "party": "codex", "permission_policy": "read-only source export at filesystem layer; workspace-write sandbox limited to controller invocation output/build path", "provider": "openai", "reasoning_effort": "high", "requested_model": "gpt-5.6-terra", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}, "opus": {"authentication_mode": "subscription OAuth state exposed only to the local wrapper; safe mode and settings sources disabled", "author_relationship": "author-independent", "cli_version": "claude-code 2.1.235", "command_sha256": "2107038579b81e9434ce9eb3b5f185939951bc78b3a675d4a1e8435a55f7fa27", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "claude-opus-5", "isolation_mode": "advisory", "party": "opus", "permission_policy": "read-only source export; result path controller-owned; Read/Grep/Glob plus test-only Bash allowlist", "provider": "anthropic", "reasoning_effort": "high", "requested_model": "claude-opus-5", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}}
- source-manifest-sha256: {"codex": "bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3", "opus": "55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df"}

## MSG-20 | 2026-08-20T21:09:53+00:00 | from: opus | type: verdict | thread: plan-v080-part2-6 | refs: -

## Verification run

Command, run literally from the pinned export root `.../exports/36852335ff92d8c97db83721bfa8e99741ed1cfe/opus`:

`python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py`

Result: `111 passed in 8.72s` (no failures, errors, or skips reported).

Tooling note, per the docket's clause: my sandbox refused `diff` on the docket path (Bash permission denial), so I read both versions in full and compared section by section, then cross-checked the comparison mechanically with line-offset anchors on every section heading and with targeted content searches. Offsets are uniform in unchanged regions (+13 after the new `Amended:` block through `### 3.1`; +15 from `- I2.` through `### 3.4`; +26 from `### 3.5`; +28 through the Slice C steps; +32 from the Slice C tests to EOF), which pins the edits to the regions named below.

## True change set (computed by comparison, not from the fold list)

1. New fifth `Amended:` header block, lines 69-80 (+13 lines) — fold 4.
2. I1, lines 295-303 (+2 lines): adds "for catalogued seats on verified flags; for operator-declared seats on the operator's declaration, recorded as declared and never verified, exactly as I4 treats a declared model" — fold 3. I2-I10 are byte-identical to round 4 (lines 304-339 vs round-4 289-324).
3. 3.4 "Known flags or refusal", lines 408-435: three optional `Seat` fields incl. `config_home`; `seats add ... [--config-home VAR=<dir…>]`; "ADMISSION IS THE FLAGS RULE ALONE"; "a pair means both lists non-empty"; "The configuration home is NOT an admission condition"; both lists asserted for a catalogued AND a declared seat; bridge exits 2 on an empty isolation list OR an empty no-persistence list — folds 1 and 2.
4. 3.4 sandbox-boundary bullet, lines 443-483: the round-4 exclusion sentence ("a vendor whose CLI has no such documented variable is not served by the generic bridge") is gone — confirmed absent by search — replaced by the optional-pointer paragraph (`--config-home` for manual seats; pointer-less seat runs under the sandboxed HOME; `seats smoke`; custom-seat-command "at smoke time, never at admission"); "Why:" → "Why the pointer exists:"; doctor/provenance gain `configuration home SANDBOX` / `- configuration-home: sandbox` — fold 1.
5. 3.4 Invocation bullet, lines 498-504: "the seat's `isolation_argv` and `no_persistence_argv` — catalogued for catalog seats, declared for manual seats, identical treatment from here on"; pointer applied "when one is catalogued or declared"; one sentence reflowed — folds 2 and 4.
6. Slice C step 1 (640-645, +2) and step 5 (`Seat.config_home`, `--config-home`) — folds 2 and 1.
7. Slice C tests: bridge bullet (catalogued AND declared seat on every invocation; exit 2 when either list is empty; pointer present for a declaring manual seat and absent — HOME stays the sandbox — for one that declares none); open/admission bullet ("with or without a declared config home"; "a manual seat declaring only one of the two lists is refused") — folds 1 and 2.
8. Slice C acceptance (747): "and no configuration-home pointer (the wrapper carries its own key)"; §5 files (869): `--config-home`; O1 (931): "the configuration home is optional, never a condition" — fold 1.

No artifact edit outside the author's fold list: every difference above maps to a listed fold, and sections 1, 2, 3.2, 3.3, 3.5-3.8, Slices A/B/D, 6, 7, 9 and the O2-O19 rows are unchanged (heading offsets confirm identical line counts; full read confirms identical text).

## (a) Folds resolve their round-4 findings / (b) reverse check

- **F1 / codex #1 (config home as a second admission condition): RESOLVED.** The single admission condition now reads consistently in all six places criterion 2 names: 3.4 (422 "ADMISSION IS THE FLAGS RULE ALONE", 428 "The configuration home is NOT an admission condition"), Scope (93-96), 1b (128-135), O1 (931), the refusal wording (423-427, flags only), and the Slice C acceptance (742-751). A search for every occurrence of "config home / configuration home / configuration-home / pointer" in the current text returns no place where a pointer is required for admission; the round-4 exclusion sentence is gone. I6 (320-325) already scopes the pointer to "may", consistent with optional. I10 (333-339) is untouched and names only the flags rule.
- **F2 / codex #2 (both argv lists sourced from the catalog only; belt-and-braces covered only the no-persistence list; tests pinned claude/codex): RESOLVED.** Invocation bullet 498-500 and Slice C step 1 640-645 source both lists from the seat (catalogued or declared, "identical treatment"); 433-435 makes the bridge exit 2 on EITHER empty list; tests 695-699 and 708-711 assert both lists for a catalogued AND a declared seat, the either-empty refusal, the one-list-only manual seat refused, and the pointer present/absent cases. That closes the enforcement gap codex #2 named (criterion 3): admission requires two non-empty lists and the bridge independently refuses to invoke if either arrives empty.
- **Observation (empty declared list unstated): RESOLVED** — 422-423 "a pair means both lists non-empty", plus the either-empty exit and the one-list-only refusal test.
- **Observation (I1's absolute claim needs I4's hedge): RESOLVED** — I1 301-303.
- No round-4 finding is left without a fold.

## (c) Coherence sweep

No change contradicts another part of the artifact. Specifically checked: the declared path is specified end to end — declaration (3.4 415-421, Slice C step 5, §5 files) → admission (3.4 422-427) → invocation (498-504, step 1) → doctor/provenance (429-430, 469-474) → tests (695-718) — and the same-vendor bullet (436-442), 3.3, 3.6's precedence chain (569-574) and O19 remain true under the optional pointer.

Standing criteria not implicated by the change set or the sweep are cited to **MSG-16** (which cited MSG-12/MSG-8 where relevant): criterion 1 (all of section 2 — verified unchanged, identical line count and text), the absolute statements I2, I10, 2.13, §6, O12, criterion 3's §5-has-no-exception-path finding, and criterion 5. Because folds 1-2 touch the isolation/persistence argument I re-established two of those on fresh evidence anyway: `collab/PROTOCOL.md:115-130` in this docket reads exactly as 2.13 quotes it ("Settings sources, hooks, plugins, MCPs and session persistence are off." at 119; the advisory paragraph at 123-126) with no waiver clause, and `src/debate/controller.py:38-61` in the export is `_RESERVED_ENV` beginning `CLAUDE_CONFIG_DIR`, `CODEX_HOME` as 2.12 states.

## Non-blocking observations (no blocking finding found in this pass)

1. **E2E fixture, line 736-740.** The fake vendor is described as registered "with isolation and no-persistence flags", yet the asserted provenance is `configuration-home: operator (...)`. Under the now-optional pointer that assertion only follows if the test-catalog entry also carries a `config_home` (which Slice C step 4 permits). Under-specified fixture, not a contradiction.
2. **I10 (338-339)** still states the belt-and-braces refusal for a missing no-persistence flag only, while 3.4 (433-435) now exits 2 on either empty list. I10 remains true (a subset), but the invariant would be better widened to match.
3. **`--config-home VAR=<dir>` (417-418)** leaves the variable NAME in the operator's hands with no stated refusal for a sandbox/`_RESERVED_ENV` key (e.g. `HOME`, `XDG_CONFIG_HOME`), which the seat-side statement "What stays sandboxed for the seat: `HOME`, `XDG_*`, `TMPDIR`" assumes. I6 (321-323) scopes the pointer to "that vendor's DOCUMENTED environment variable", so this is a declaration-model gap of the same kind as a bogus declared isolation flag, not an internal contradiction; an explicit refusal in Slice C step 5 would close it.
4. **Line 457-458**, "the env-pinned wrappers in the catalog notes already do (they carry their keys inside the script)": the export's notes (`src/debate/seat_catalog.py:78-111`) document MODEL env-pinning ("wrapper env-pins ANTHROPIC_MODEL=glm-5.3", "wrapper env-pins deepseek-v4-flash"), not credential carrying. The credential half is a host-level claim about the owner's wrapper scripts — the class the docket removes from the vote — and the design does not rest on it (the `seats smoke` fallback at 458-460 covers a tool that cannot authenticate), so it is an attribution imprecision rather than a dishonest capability claim.
5. The "old registries without the field load" test (732-733) is named for `capability_class` only, not for the three new optional fields; 2.6's `_seat_from_raw` tolerance covers the mechanism.

PASS: the exact command passes, every round-4 finding is resolved by a fold in the true change set, no unlisted artifact edit exists, and criteria 1-5 hold (standing by citation to MSG-16 where unimplicated).

Controller-Decision:
- decision: PASS

Controller-Sealed-Reveal:
- reveal-id: 977c9e8872e328d1dceb91bdcc5f11f2f1d7b7f5ae9ce1f7b6dcad9e7423b065
- phase: sealed
- captured-at: 2026-08-20T21:07:25+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: 71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d
- controller-config-sha256: d2b5119174a1b97a64a15e1fd67f980ba89b03f1af0399a5aecc890073c7c9bb
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: 55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df
- docket-revision-sha256: 75cc1efb12cffe216448882a40e3f962075425a2a888372dc6afcb6feffa98e1
- input-sha256: 65bf6924b516e681bb3a9f0f454015ec9a86ca7047db58792b0feaae6a4c80be
- requested-model: claude-opus-5
- runtime-model: claude-opus-5
- reasoning-effort: high
- cli-version: claude-code 2.1.235
- isolation-mode: advisory

## MSG-21 | 2026-08-20T21:09:53+00:00 | from: codex | type: verdict | thread: plan-v080-part2-6 | refs: -

Blocking finding: the round-4 fold newly makes manual `--config-home VAR=<dir>` declarable, but specifies no validation of either `VAR` or the relative path. A declared `HOME=.config` (or `XDG_*`/`TMPDIR`) would cause the bridge—below the profile-level `_RESERVED_ENV` guard—to replace a controller-sandboxed variable with a path under the real home; traversal/absolute-path handling is likewise unspecified. This contradicts §3.4's claim that HOME, XDG_*, and TMPDIR remain sandboxed, and leaves no required negative test. Restrict the variable to a documented non-controller-owned config variable, reject `_RESERVED_ENV` names, validate containment beneath the real home, and test those refusals. The true-change diff confirms this manual `config_home` declaration is newly added. Required command run exactly: `python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py` → `111 passed in 9.86s`. Unimplicated section-2 and protocol standing checks are cited to MSG-16 as the docket directs.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: 977c9e8872e328d1dceb91bdcc5f11f2f1d7b7f5ae9ce1f7b6dcad9e7423b065
- phase: sealed
- captured-at: 2026-08-20T21:09:53+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8
- controller-config-sha256: d2b5119174a1b97a64a15e1fd67f980ba89b03f1af0399a5aecc890073c7c9bb
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3
- docket-revision-sha256: 75cc1efb12cffe216448882a40e3f962075425a2a888372dc6afcb6feffa98e1
- input-sha256: ce127b3ee7ac17444533185d3e0bb1243c3723ac9aab4c8cff33877f3b51cffa
- requested-model: gpt-5.6-terra
- runtime-model: gpt-5.6-terra
- reasoning-effort: high
- cli-version: codex-cli 0.148.0
- isolation-mode: advisory

## MSG-22 | 2026-08-20T21:15:31+00:00 | from: opus | type: verdict | thread: plan-v080-part2-6 | refs: -

## Verification run

Command, run literally from the pinned export root `.../exports/36852335ff92d8c97db83721bfa8e99741ed1cfe/opus`:

`python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py`

Result: `111 passed in 8.63s` (no failures, errors, or skips). The command is not the blocker.

Tooling note (docket clause): my sandbox again refused `diff` on the docket path, so I read both versions in full, compared section by section, and re-checked the comparison mechanically with heading line-offsets: +13 through `### 3.1`, +15 at `### 3.2`, +26 at `### 3.5` and Slice C, +32 at Slice A and `## 5. Files`. Edits are confined to the header block, I1, 3.4, and Slice C — the change set I described at MSG-20 items 1-8, which codex's independent diff also reports.

## Position change on MSG-21

I sealed PASS at MSG-20 with the `--config-home VAR=<dir>` gap listed as non-blocking observation 3, on the reasoning that an unvalidated declared VAR is the same class of operator assertion as a bogus declared isolation flag. Fresh evidence from the round-4 comparison shows that reasoning is wrong, and codex's MSG-21 finding is blocking. I now vote NO_PASS.

**Blocking finding (concurring with MSG-21, on my own evidence).** In round 4 the configuration-home variable could only come from the catalog: `plan-round4.md:629` lists `config_home` among catalog fields "(verified strings only; the notes say where each was read)", and round 4 has no `Seat.config_home` and no `seats add --config-home` (grep over `plan-round4.md` for `config[- _]home` returns only the engine flag at 379, the catalog field at 629, and prose/doctor/provenance lines). The name was therefore a vendor-documented, non-reserved string by construction, which is exactly what made two unhedged claims true:

- `plan-round4.md:443-444` = current `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md:467-468`, unchanged and unhedged: "What stays sandboxed for the seat: `HOME`, `XDG_*`, `TMPDIR`, build paths, the read-only source export, the detached stdin."
- I6 (current 320-325), unchanged: the pointer goes through "that vendor's **DOCUMENTED** environment variable".

Fold 1 removes that construction guarantee — current 414-418 add `Seat.config_home` and `seats add ... [--config-home VAR=<dir relative to the operator's home>]` under "the operator asserts what their tool does ... never verified", and 453-454 / 502 apply the declared pair to the seat environment one process below the `_RESERVED_ENV` guard — and it puts nothing in the guarantee's place. No refusal is stated anywhere for a declared `VAR` that is `HOME`, an `XDG_*` name, `TMPDIR`, or any `_RESERVED_ENV` key (`src/debate/controller.py:38-61` in the export, beginning `CLAUDE_CONFIG_DIR`, `CODEX_HOME`), and no constraint is stated on the `<dir>` side (absolute path, `..` traversal). A declared `HOME=.config` therefore replaces the sandboxed HOME with a path under the real home for the seat process, which falsifies 467-468 verbatim and voids I6's "documented variable" scope. This is not the declared-flags class: whether a vendor flag really disables plugins is outside the engine's power to verify, but the variable NAME is a string the engine itself writes into the child environment and can check without any vendor knowledge. Leaving it unchecked also undercuts the sandbox-boundary argument at 443-467, whose whole force is that the profile-level refusal of `_RESERVED_ENV` still holds and only the vendor's documented variable crosses the line. Criterion 2 (internal coherence) and criterion 3 (invariants complete and non-contradictory) fail on the change set itself, and the Slice C test list (697-699, 710) requires only present/absent pointer cases — no negative test.

To close it: restrict `--config-home` to a non-controller-owned configuration variable, refuse `_RESERVED_ENV` names and the sandbox names 467-468 asserts (`HOME`, `XDG_*`, `TMPDIR`), require the directory to resolve beneath the real home (reject absolute paths and `..`), state the refusal in 3.4 and Slice C step 5, and add the refusal tests to the Slice C list.

No other blocking finding is establishable in this pass; a second pass by me would find nothing new.

## Everything else in the round-4 fold check still holds

- **F1 / codex #1 (pointer as a second admission condition): resolved.** One admission condition in all six places criterion 2 names — 3.4 (422 "ADMISSION IS THE FLAGS RULE ALONE", 428 "The configuration home is NOT an admission condition"), Scope (93-96), 1b (128-135), O1 (931), the refusal wording (423-427, flags only), Slice C acceptance (742-751). Round 4's exclusion sentence ("a vendor whose CLI has no such documented variable is not served by the generic bridge", `plan-round4.md:434-436`) is gone; a search for every config-home mention in the current text finds no place requiring a pointer for admission.
- **F2 / codex #2 (both lists catalog-only; belt-and-braces one-sided; tests pinned to claude/codex): resolved.** 498-500 and Slice C step 1 (640-645) source both lists from the seat, "catalogued for catalog seats, declared for manual seats, identical treatment"; 433-435 exits 2 on EITHER empty list; 694-699 and 708-711 assert both lists for a catalogued AND a declared seat, the either-empty refusal, the one-list-only manual refusal, and the pointer present/absent cases. That closes the enforcement gap codex #2 named.
- **Observation (empty declared list): resolved** — 422-424 "a pair means both lists non-empty" plus the either-empty exit and the one-list-only test. **Observation (I1 hedge): resolved** — I1 301-303. Every round-4 finding has a fold; no artifact edit falls outside the fold list (I2-I10, sections 1, 2, 3.2, 3.3, 3.5-3.8, Slices A/B/D, 6, 7, 9, O2-O19 are byte-identical; offsets and full read agree).

Standing round-4 criteria the change set and sweep leave unimplicated are cited to **MSG-16** (which cited MSG-12/MSG-8 where relevant): criterion 1 (all of section 2), the absolute statements I2, I10, 2.13, §6, O12, criterion 3's §5-no-exception-path finding, and criterion 5. Two of these I re-established on fresh evidence because the folds touch the isolation argument: `collab/PROTOCOL.md:115-130` in this docket reads exactly as 2.13 quotes it ("Settings sources, hooks, plugins, MCPs and session persistence are off." at 119, advisory paragraph 123-126, no waiver clause), and `src/debate/controller.py:38-61` is `_RESERVED_ENV` beginning `CLAUDE_CONFIG_DIR`, `CODEX_HOME` as 2.12 states.

## Non-blocking observations (carried from MSG-20, unchanged)

1. E2E fixture 734-740: the fake vendor is described as registered "with isolation and no-persistence flags" yet asserts `configuration-home: operator (...)`; under the now-optional pointer that follows only if the test-catalog entry also carries a `config_home` (permitted by step 4). Under-specified, not contradictory.
2. I10 (338-339) still states the belt-and-braces refusal for a missing no-persistence flag only, while 433-435 exits 2 on either empty list. I10 stays true as a subset; widening it would match.
3. Line 457-458 "(they carry their keys inside the script)": the export's catalog notes (`src/debate/seat_catalog.py:78-111`) document MODEL env-pinning, not credential carrying. The credential half is a host-level claim about the owner's wrappers — the class the docket removes from the vote — and the design does not rest on it (the `seats smoke` fallback at 458-460 covers a tool that cannot authenticate), so it is attribution imprecision, not a dishonest capability claim.
4. The "old registries without the field load" test (732-733) names `capability_class` only, not the three new optional fields; 2.6's `_seat_from_raw` tolerance covers the mechanism.

Controller-Decision:
- decision: NO_PASS

Controller-Provenance:
- phase: deliberation
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: 71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d
- controller-config-sha256: d2b5119174a1b97a64a15e1fd67f980ba89b03f1af0399a5aecc890073c7c9bb
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: 55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df
- docket-revision-sha256: 75cc1efb12cffe216448882a40e3f962075425a2a888372dc6afcb6feffa98e1
- input-sha256: ff964237db92c2d9471d6c12f923f3ccd6d104e441af18df68e780c8574725d6
- requested-model: claude-opus-5
- runtime-model: claude-opus-5
- reasoning-effort: high
- cli-version: claude-code 2.1.235
- isolation-mode: advisory

## MSG-23 | 2026-08-20T21:15:31+00:00 | from: owner | type: close | thread: plan-v080-part2-6 | refs: -

Controller closed the managed case as NO_PASS. Reason: party-vote-agreement. Supervisor messages were not counted as party votes.

Controller-Terminal:
- terminal-result: NO_PASS
- close-reason: party-vote-agreement

## MSG-24 | 2026-08-20T21:17:30+00:00 | from: owner | type: review-request | thread: plan-v080-part2-7 | refs: feature/installation-onboarding-v080@36852335ff92d8c97db83721bfa8e99741ed1cfe

Fix-report and round-6 review request (fold-delta) for the v0.8.0 part-2 plan doc `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md`. Round 5 (case `plan-v080-part2-6`) closed NO_PASS at MSG-23: opus sealed PASS (MSG-20), codex NO_PASS (MSG-21) on the unvalidated declarable configuration-home pointer, opus concurred in deliberation (MSG-22). Every finding has been folded; the docket restates each because the channel record is outside the seats' docket:

1. The pointer is validated: the variable is either a catalogued vendor variable (`CLAUDE_CONFIG_DIR`, `CODEX_HOME`) or a well-formed name outside `_RESERVED_ENV` and outside the sandbox set (HOME, XDG_*, TMPDIR/TEMP/TMP, PATH, PYTHONPATH); the directory is relative, free of `..`, beneath the operator's home; refused at `seats add`, re-checked by `_brokered_adapter` at open and by the bridge at invocation (exit 2); an explicit refusal/acceptance test matrix is named. I6, I10, the "what stays sandboxed" sentence and Slice C step 5 carry the rule.
2. MSG-20 observations folded: the E2E fixture's vendor carries a `config_home`; I10 widened to either-empty list; the wrapper-credential sentence cites the registry's own self-sourcing rule (`seats.py:483-487`); the old-registry test names all four optional fields.

The round-5 artifact version is materialized in the docket as `plan-round5.md`; compute the true change set yourself per the docket's fold-delta clause. Verdicts cite your own fresh evidence; standing round-5 criteria may be cited by MSG id where your diff and sweep leave them unimplicated.


Controller-Docket-Provenance:
- topology: recommended-three-agent
- controller-config-sha256: 03233f08f964b9e17511b194abf62486080923e33748d44388df45360f942bdb
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- docket-revision-sha256: ad62a562f1ca68bfdff23b54e004766f63eedddddb7b8a641bbf681c01784f3b
- docket-files: [{"path": "collab/PROTOCOL.md", "sha256": "dfb3950a24d38ac308632baa117e961ae9da00ed1b8aac8a48261d8cbeaeb9c6", "tracked_at_source_ref": true}, {"path": "collab/plan-v080-part2-63227.debate.json", "sha256": "24740b4f553046288ff8d69a939e8bde3959b09183bc06c34addb936440e7a49", "tracked_at_source_ref": false}, {"path": "docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md", "sha256": "2aec7cf7ab6d1023aa6f05ef7b4d281eaae951843f6f7a6ed4aacd0cebed89d6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-round5.md", "sha256": "2aec7cf7ab6d1023aa6f05ef7b4d281eaae951843f6f7a6ed4aacd0cebed89d6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-docket-r6.md", "sha256": "cbf2bd468ad213e7f4ae292172cece2fc4da30c5ac905e3e664cdc774810da2a", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat-result.schema.json", "sha256": "0f03c5e1f0be74c320750c3c2baf5a5a14c39622e34babe8c0b30781578161b1", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat_adapter.py", "sha256": "5e3f30bd1d0e3dd0b2ff605bc3b4412970912744c8456f944af6128ad46e71a6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/watcher-r6.json", "sha256": "03233f08f964b9e17511b194abf62486080923e33748d44388df45360f942bdb", "tracked_at_source_ref": false}]
- profile-sha256: {"codex": "c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8", "opus": "71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d"}
- sanitized-profile-manifests: {"codex": {"authentication_mode": "Codex subscription OAuth state exposed only to the local wrapper; user config and rules ignored", "author_relationship": "author-independent", "cli_version": "codex-cli 0.148.0", "command_sha256": "bc02f45ab947a2ea025ac951b33fdc4235b974411683e1505590178a869f8667", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "gpt-5.6-terra", "isolation_mode": "advisory", "party": "codex", "permission_policy": "read-only source export at filesystem layer; workspace-write sandbox limited to controller invocation output/build path", "provider": "openai", "reasoning_effort": "high", "requested_model": "gpt-5.6-terra", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}, "opus": {"authentication_mode": "subscription OAuth state exposed only to the local wrapper; safe mode and settings sources disabled", "author_relationship": "author-independent", "cli_version": "claude-code 2.1.235", "command_sha256": "2107038579b81e9434ce9eb3b5f185939951bc78b3a675d4a1e8435a55f7fa27", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "claude-opus-5", "isolation_mode": "advisory", "party": "opus", "permission_policy": "read-only source export; result path controller-owned; Read/Grep/Glob plus test-only Bash allowlist", "provider": "anthropic", "reasoning_effort": "high", "requested_model": "claude-opus-5", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}}
- source-manifest-sha256: {"codex": "bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3", "opus": "55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df"}

## MSG-25 | 2026-08-20T21:18:17+00:00 | from: owner | type: close | thread: plan-v080-part2-7 | refs: -

Supervisor close before any seat invocation: this case was opened against an UNFOLDED artifact (the round-5 fold script aborted before writing; the plan in this docket is byte-identical to plan-round5.md). No seat ran, nothing was reviewed, no verdict exists. The folded artifact re-opens as the next case on this channel.

## MSG-26 | 2026-08-20T21:18:31+00:00 | from: owner | type: review-request | thread: plan-v080-part2-8 | refs: feature/installation-onboarding-v080@36852335ff92d8c97db83721bfa8e99741ed1cfe

Note on MSG-24/25: case `plan-v080-part2-7` was opened by mistake against the UNFOLDED round-5 artifact (the fold script aborted before writing) and was closed by the supervisor at MSG-25 before any seat ran; no verdict exists for it. This case carries the folded artifact.

Fix-report and round-6 review request (fold-delta) for the v0.8.0 part-2 plan doc `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md`. Round 5 (case `plan-v080-part2-6`) closed NO_PASS at MSG-23: opus sealed PASS (MSG-20), codex NO_PASS (MSG-21) on the unvalidated declarable configuration-home pointer, opus concurred in deliberation (MSG-22). Every finding has been folded; the docket restates each because the channel record is outside the seats' docket:

1. The pointer is validated: the variable is either a catalogued vendor variable (`CLAUDE_CONFIG_DIR`, `CODEX_HOME`) or a well-formed name outside `_RESERVED_ENV` and outside the sandbox set (HOME, XDG_*, TMPDIR/TEMP/TMP, PATH, PYTHONPATH); the directory is relative, free of `..`, beneath the operator's home; refused at `seats add`, re-checked by `_brokered_adapter` at open and by the bridge at invocation (exit 2); an explicit refusal/acceptance test matrix is named. I6, I10, the "what stays sandboxed" sentence and Slice C step 5 carry the rule.
2. MSG-20 observations folded: the E2E fixture's vendor carries a `config_home`; I10 widened to either-empty list; the wrapper-credential sentence cites the registry's own self-sourcing rule (`seats.py:483-487`); the old-registry test names all four optional fields.

The round-5 artifact version is materialized in the docket as `plan-round5.md`; compute the true change set yourself per the docket's fold-delta clause. Verdicts cite your own fresh evidence; standing round-5 criteria may be cited by MSG id where your diff and sweep leave them unimplicated.


Controller-Docket-Provenance:
- topology: recommended-three-agent
- controller-config-sha256: 03233f08f964b9e17511b194abf62486080923e33748d44388df45360f942bdb
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- docket-revision-sha256: 76277921029d85bcfb008ebbdd7194e82baf7cd2a2bfefb41a8f8bbbcb5a2c22
- docket-files: [{"path": "collab/PROTOCOL.md", "sha256": "dfb3950a24d38ac308632baa117e961ae9da00ed1b8aac8a48261d8cbeaeb9c6", "tracked_at_source_ref": true}, {"path": "collab/plan-v080-part2-63227.debate.json", "sha256": "24740b4f553046288ff8d69a939e8bde3959b09183bc06c34addb936440e7a49", "tracked_at_source_ref": false}, {"path": "docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md", "sha256": "075c15971b48aea10bfc593fe8f0cfec7c7e6d468e7b7982c759f21b259dcbde", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-round5.md", "sha256": "2aec7cf7ab6d1023aa6f05ef7b4d281eaae951843f6f7a6ed4aacd0cebed89d6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-docket-r6.md", "sha256": "cbf2bd468ad213e7f4ae292172cece2fc4da30c5ac905e3e664cdc774810da2a", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat-result.schema.json", "sha256": "0f03c5e1f0be74c320750c3c2baf5a5a14c39622e34babe8c0b30781578161b1", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat_adapter.py", "sha256": "5e3f30bd1d0e3dd0b2ff605bc3b4412970912744c8456f944af6128ad46e71a6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/watcher-r6.json", "sha256": "03233f08f964b9e17511b194abf62486080923e33748d44388df45360f942bdb", "tracked_at_source_ref": false}]
- profile-sha256: {"codex": "c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8", "opus": "71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d"}
- sanitized-profile-manifests: {"codex": {"authentication_mode": "Codex subscription OAuth state exposed only to the local wrapper; user config and rules ignored", "author_relationship": "author-independent", "cli_version": "codex-cli 0.148.0", "command_sha256": "bc02f45ab947a2ea025ac951b33fdc4235b974411683e1505590178a869f8667", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "gpt-5.6-terra", "isolation_mode": "advisory", "party": "codex", "permission_policy": "read-only source export at filesystem layer; workspace-write sandbox limited to controller invocation output/build path", "provider": "openai", "reasoning_effort": "high", "requested_model": "gpt-5.6-terra", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}, "opus": {"authentication_mode": "subscription OAuth state exposed only to the local wrapper; safe mode and settings sources disabled", "author_relationship": "author-independent", "cli_version": "claude-code 2.1.235", "command_sha256": "2107038579b81e9434ce9eb3b5f185939951bc78b3a675d4a1e8435a55f7fa27", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "claude-opus-5", "isolation_mode": "advisory", "party": "opus", "permission_policy": "read-only source export; result path controller-owned; Read/Grep/Glob plus test-only Bash allowlist", "provider": "anthropic", "reasoning_effort": "high", "requested_model": "claude-opus-5", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}}
- source-manifest-sha256: {"codex": "bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3", "opus": "55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df"}

## MSG-27 | 2026-08-20T21:26:29+00:00 | from: opus | type: verdict | thread: plan-v080-part2-8 | refs: -

PASS.

## Verification command

Run literally from the pinned export root (/home/zoltan/Projects/debate/var/debate/plan-v080-part2-63227/controller/exports/36852335ff92d8c97db83721bfa8e99741ed1cfe/opus):

`python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py`

Result: `111 passed in 9.01s` (72 dots then 39 dots, 64%/100% progress lines). No failures, no errors.

## Tooling note (docket line 24-25)

My sandbox refused `diff`: `diff -u <docket>/var/debate/plan-v080-part2-63227/plan-round5.md <docket>/docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md` returned "Permission to use Bash has been denied" (the shell is confined to the export root; the docket tree is outside it — `ls` of the docket directory was refused the same way, while `ls` of the export root succeeded). Per the docket's instruction I read BOTH versions in full (round-5: 976 lines, in three reads covering 1-976; current: 1019 lines, in two reads covering 1-1019) and compared section by section, anchoring the comparison on line-count offsets at every section and bullet boundary. The offset ledger closes exactly: +43 total, decomposed as +14 (new Amended block), +2 (I6), +1 (I10), +13 (3.4 "Known flags or refusal"), +2 (3.4 sandbox-boundary bullet), +2 (Slice C step 5), +2 (bridge test bullet), +5 (open/admission test bullet), +2 (pairing test bullet) = 43, with every section header landing at its predicted line (round-5 152/291/391/517/549/586/610/628/753/863/888/904/927/951 → current 166/305/408/549/581/618/642/660/796/906/931/947/970/994). Two in-place edits carry no line delta (the sandbox-boundary wrapper sentence, the E2E fixture bullet) and are both in the fold list.

## True change set (computed, not taken from the fold list)

1. New sixth `Amended:` block, lines 82-94 (round-5 folds, MSG-20/22/21, close MSG-23) — fold 6.
2. I6, current 334-341 vs round-5 320-325: gains "VALIDATED (an operator-declared variable can never name a controller-owned or sandbox variable, and its directory can never leave the operator's home — 3.4)" — fold 1.
3. I10, current 349-356 vs round-5 333-339: belt-and-braces widened from "without a no-persistence flag" to "an empty isolation list, an empty no-persistence list, or a configuration-home pointer that fails the 3.4 validation" — folds 1 and 3 (MSG-20 obs 2).
4. 3.4 "Known flags or refusal", current 437-450 inserted: "ONE thing IS verified ... the pointer. `VAR` must be either one of the catalog's documented vendor configuration-home variables (`CLAUDE_CONFIG_DIR`, `CODEX_HOME`) or a name matching `[A-Z][A-Z0-9_]*` that is NOT in `_RESERVED_ENV` (2.12) and NOT a sandbox variable (`HOME`, `XDG_CONFIG_HOME`, `XDG_CACHE_HOME`, `XDG_DATA_HOME`, `TMPDIR`, `TEMP`, `TMP`, `PATH`, `PYTHONPATH`); `<dir>` must be relative, contain no `..` segment, and resolve beneath the operator's home", with refusal at `seats add`, re-check by `_brokered_adapter` at open, and bridge re-check against `DEBATE_BRIDGE_REAL_HOME` with exit 2 and no seat call — fold 1.
5. 3.4 sandbox boundary, current 486-489: the wrapper sentence now reads "the registry's own rule already requires exactly that shape of wrapper ("credentials belong in a self-sourcing wrapper, never the registry", `seats.py:483-487`)", replacing round-5's "the env-pinned wrappers in the catalog notes already do (they carry their keys inside the script)" — fold 4 (MSG-20 obs 3).
6. 3.4 "What stays sandboxed", current 498: gains "— and a declared pointer can name none of these (the validation above)" — fold 1.
7. Slice C step 5, current 695-697: `add_seat` "validates `--config-home` per 3.4" — fold 1.
8. Bridge test bullet, current 733-736: bridge exits 2 without a seat call when the pointer names a reserved or sandbox variable or a directory outside `DEBATE_BRIDGE_REAL_HOME` — fold 1.
9. Open/admission test bullet, current 747-753: the explicit matrix (`HOME=.config`, `XDG_CONFIG_HOME=x`, `TMPDIR=t`, `PATH=p`, `GIT_DIR=g`, lowercase/malformed name, absolute directory, `..` segment refused; `CLAUDE_CONFIG_DIR=.claude` and `MYTOOL_HOME=.mytool` accepted; hand-edited invalid registry entry refused by `_brokered_adapter`) — fold 1.
10. Pairing test bullet, current 774-776: "old registries without any of the four new optional fields (`capability_class`, `isolation_argv`, `no_persistence_argv`, `config_home`) load unchanged" — fold 5 (MSG-20 obs 4).
11. E2E bullet, current 778-779: the fake vendor now carries "isolation and no-persistence flags AND a `config_home`" — fold 2 (MSG-20 obs 1).

No artifact edit outside the fold list: the offset ledger accounts for all 43 added lines, and the two zero-delta edits are folds 4 and 2. The docket instruction sheet itself is excluded scaffolding by name.

## (a) Each fold resolves its round-5 finding

- Blocker (unvalidated declarable pointer, MSG-21/MSG-22). Resolved, and I established the substance fresh against the pinned export. `controller.py:38-61` gives `_RESERVED_ENV` = {CLAUDE_CONFIG_DIR, CODEX_HOME, HOME, XDG_CONFIG_HOME, XDG_CACHE_HOME, XDG_DATA_HOME, TMPDIR, TEMP, TMP, the nine GIT_* names, PYTEST_ADDOPTS, PYTHONDONTWRITEBYTECODE}. `controller.py:682-706` (`_adapter_environment`) writes exactly HOME, XDG_CONFIG_HOME, XDG_CACHE_HOME, XDG_DATA_HOME (694-696), TMPDIR/TEMP/TMP, GIT_CEILING_DIRECTORIES, PYTHONDONTWRITEBYTECODE, PYTEST_DISABLE_PLUGIN_AUTOLOAD, PYTEST_ADDOPTS. Union of the plan's two exclusion sets (`_RESERVED_ENV` ∪ {HOME, XDG_CONFIG_HOME, XDG_CACHE_HOME, XDG_DATA_HOME, TMPDIR, TEMP, TMP, PATH, PYTHONPATH}) covers every one of those except PYTEST_DISABLE_PLUGIN_AUTOLOAD (observation 3 below). The round-5 falsification is therefore repaired by construction: `HOME=.config`, any `XDG_*` the controller actually derives, `TMPDIR`, `PATH`, `PYTHONPATH` and every `_RESERVED_ENV` key are refused, and `<dir>` cannot be absolute, cannot contain `..`, and must resolve beneath the operator's home. The negative tests that did not exist in round 5 now exist at three layers (`seats add` matrix, `_brokered_adapter` hand-edited-registry refusal, bridge exit-2), and I6's "documented variable" scope is restored ("VALIDATED"). PATH/PYTHONPATH are correctly added beyond `_RESERVED_ENV` — the bridge profile itself sets PYTHONPATH (3.4, current 417).
- MSG-20 obs 1: E2E fixture now declares a `config_home`, so its assertion `configuration-home: operator (...)` (current 781-782) is now sourced. Resolved.
- MSG-20 obs 2: I10 belt-and-braces now covers both empty lists and the pointer, matching 3.4's "exits 2 ... with an empty `--isolation-argv-json` OR an empty `--no-persistence-argv-json`" and the pointer re-check. Resolved.
- MSG-20 obs 3: established fresh in the export. `src/debate/seats.py:482-487` is a `for part in argv:` loop raising `refused: command looks credential-shaped; credentials belong in a self-sourcing wrapper, never the registry`. The plan quotes that string verbatim and attributes it to the registry rather than to the catalog notes; the attribution is now accurate. Resolved. (Criterion 1's one new citation, established fresh as the docket requires.)
- MSG-20 obs 4: pairing test names all four optional fields. Resolved.
- Bookkeeping: the sixth `Amended:` line is present, records the folds with channel provenance (MSG-20/22 opus, MSG-21 codex, MSG-23 close NO_PASS), and its summary of the rule matches 3.4 verbatim in substance.

## (b) Reverse check — every round-5 finding has a fold

Blocker → items 2,3,4,6,7,8,9. Obs 1 → item 11. Obs 2 → item 3. Obs 3 → item 5. Obs 4 → item 10. Bookkeeping → item 1. No round-5 finding is without a fold.

## (c) Coherence sweep and standing criteria

Criterion 2 (the docket's named consistency set): 3.4 states the rule normatively; I6, I10, Slice C step 5 and the two test bullets all cite it and none contradicts its operative content. The three enforcement points (seats add / `_brokered_adapter` at open / bridge at invocation) each have a matching test bullet. "What stays sandboxed for the seat" is now true by construction for every variable `_adapter_environment` derives (verified above against controller.py:689-704).

Criterion 3: PROTOCOL.md §5 re-read in the docket copy at lines 115-126 — "Settings sources, hooks, plugins, MCPs and session persistence are off." and the `isolation_mode: advisory` paragraph, with no exception path and no waiver clause; 2.13 and I10 quote it correctly. The profile-level guard is untouched by the change set (§5 "the `_RESERVED_ENV` guard UNCHANGED", §6 "no change to `_RESERVED_ENV` or the profile-level guard", O19) and `controller.py:38-61` is unmodified in the export. The declared pointer cannot reach a sandbox variable or leave the operator's home, per the fresh evidence above.

Criteria implicated by the change set are established fresh above. Unimplicated criteria stand by citation to MSG-22 (and MSG-16/12/8 where MSG-22 cited them), as the docket permits: the resolution of round-4 F1/F2 and both observations; all of section 2 (verified unchanged by the offset ledger — round-5 152-289 maps to current 166-303 with zero delta); the absolute statements I2, 2.13, §6, O12; criterion 3's §5-no-exception-path finding; criterion 5 (§7 unchanged, round-5 904-925 → current 947-968, zero delta). I did not vote on the owner's field observations, the host CLI flags at the end of section 2, or the deferred field-pass measurements.

## Non-blocking observations (none blocks; recorded so a second pass finds nothing new)

1. I6 and Slice C step 5 compress the rule in a way that omits 3.4's first branch. I6 says an operator-declared variable "can never name a controller-owned or sandbox variable", and step 5 glosses the check as "variable name against `_RESERVED_ENV` and the sandbox set" — but `CLAUDE_CONFIG_DIR` and `CODEX_HOME` are the first two entries of `_RESERVED_ENV` (controller.py:39-40) and 2.12 titles that set the "Controller-owned environment guard", while 3.4 and the test matrix (current 749-750) require `CLAUDE_CONFIG_DIR=.claude` to be ACCEPTED. Not blocking: both sites explicitly delegate ("— 3.4", "per 3.4"), I6's preceding clause already sanctions "that vendor's DOCUMENTED environment variable", the accepted names are exactly the catalogued pointer's own variables (no new reach), and TDD is driven by the test bullet, so nothing builds wrong. Suggested one-line fix: I6 "can never name a sandbox variable or a controller-owned variable other than the vendor's own documented configuration home".
2. "ADMISSION IS THE FLAGS RULE ALONE" (current 451-452) and "The configuration home is NOT an admission condition" now coexist with an open-time refusal of an invalid stored pointer by `_brokered_adapter`. The intended and defensible reading is that the pointer's ABSENCE never blocks admission (the round-4 fold), while a malformed stored value is refused as an integrity failure — `seats add` prevents a well-formed registry from ever carrying one. Worth one clarifying clause.
3. `PYTEST_DISABLE_PLUGIN_AUTOLOAD` (controller.py:702) is the one variable `_adapter_environment` writes that sits in neither `_RESERVED_ENV` nor the plan's sandbox set, so a declared `PYTEST_DISABLE_PLUGIN_AUTOLOAD=<dir>` would validate. Not a hole: that name is outside the profile-level guard too, so the pointer creates no asymmetry with hand-authored adapters, and a directory-path value is still truthy, so pytest plugin autoload stays disabled. Relatedly, "XDG_*" in the sandbox sentence is shorthand for exactly the three variables at controller.py:694-696; an XDG name the controller never derives (e.g. `XDG_STATE_HOME`) would validate as an ordinary new variable beneath the operator's home, replacing nothing sandboxed.
4. `DEBATE_BRIDGE_REAL_HOME` is not in either exclusion set, so it could be declared as the pointer variable. Harmless: the bridge validates against the value in its OWN environment before composing the child environment, so the override reaches only the seat and still points beneath the operator's home.

## Bar

The exact command passes (111 passed); every round-5 finding — the blocker and all four MSG-20 observations, plus the bookkeeping line — is resolved by a fold present in the true change set; no artifact edit is absent from the fold list; criteria 1-5 hold, with the implicated ones established on fresh export evidence (seats.py:482-487, controller.py:38-61, controller.py:682-706, PROTOCOL.md:115-126) and the rest standing by citation to MSG-22. PASS.

Controller-Decision:
- decision: PASS

Controller-Sealed-Reveal:
- reveal-id: 1ed901ef215bb89868553521ccef386b2638c4fe5a42a1f660a7facedaf4a4fb
- phase: sealed
- captured-at: 2026-08-20T21:24:06+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: 71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d
- controller-config-sha256: 03233f08f964b9e17511b194abf62486080923e33748d44388df45360f942bdb
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: 55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df
- docket-revision-sha256: 76277921029d85bcfb008ebbdd7194e82baf7cd2a2bfefb41a8f8bbbcb5a2c22
- input-sha256: c0694ac9a2861ba10b3168830725ec0a4214ea0dc98d44b0e6c3dcc196e3662e
- requested-model: claude-opus-5
- runtime-model: claude-opus-5
- reasoning-effort: high
- cli-version: claude-code 2.1.235
- isolation-mode: advisory

## MSG-28 | 2026-08-20T21:26:29+00:00 | from: codex | type: verdict | thread: plan-v080-part2-8 | refs: -

Blocking coherence/safety finding: §3.4 permits `seats add --config-home` to accept `CLAUDE_CONFIG_DIR=.claude` (and tests require that acceptance), although `src/debate/controller.py:38-40` establishes `CLAUDE_CONFIG_DIR` and `CODEX_HOME` are controller-owned `_RESERVED_ENV` keys. This contradicts I6's absolute claim that an operator-declared variable can never name a controller-owned variable, and fails docket criterion 3 requiring a declared pointer be unable to reach one. Fresh inspection command `nl -ba src/debate/controller.py | sed -n '35,65p'` showed both keys in `_RESERVED_ENV`; the true diff showed the explicit acceptance and I6 wording. The new §2 citation was verified: `nl -ba src/debate/seats.py | sed -n '460,495p'` showed lines 483-487 require credentials in a self-sourcing wrapper. Exact required verification ran: `python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py` -> `111 passed in 9.19s`; passing tests do not cure the plan contradiction. Unimplicated prior checks stand by the docket's MSG-22 citation.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: 1ed901ef215bb89868553521ccef386b2638c4fe5a42a1f660a7facedaf4a4fb
- phase: sealed
- captured-at: 2026-08-20T21:26:29+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8
- controller-config-sha256: 03233f08f964b9e17511b194abf62486080923e33748d44388df45360f942bdb
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3
- docket-revision-sha256: 76277921029d85bcfb008ebbdd7194e82baf7cd2a2bfefb41a8f8bbbcb5a2c22
- input-sha256: 3c3d2d637ef0a0ed981c4c3e9d21b3c4d110d009b63c1531b3e8a385a879a2fd
- requested-model: gpt-5.6-terra
- runtime-model: gpt-5.6-terra
- reasoning-effort: high
- cli-version: codex-cli 0.148.0
- isolation-mode: advisory

## MSG-29 | 2026-08-20T21:32:53+00:00 | from: opus | type: verdict | thread: plan-v080-part2-8 | refs: -

NO_PASS. I move off my sealed PASS (MSG-27): codex's MSG-28 finding is real on the coherence axis, and on re-inspection it is worse than the non-blocking observation 1 I recorded — it reaches Slice C step 5, which is a build directive that contradicts a required test in the same document. I do not adopt codex's safety framing; see "Where MSG-28 overreaches".

## Verification command

Run literally from the pinned export root:

`python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py`

Result: `111 passed in 9.05s` (72 dots then 39 dots, 64%/100%). No failures, no errors. Passing tests do not cure a plan-text contradiction.

## Blocking finding (criterion 2, whole-document coherence — the axis the docket names for this round)

The fold states the pointer rule in three of the five sites criterion 2 enumerates in mutually exclusive terms, and the two defective sites are BOTH new fold text (round-5 `plan-round5.md:663` step 5 ends at "`add_seat` refuses `:` in a submodel." with no validation clause; round-5 I6 has no "VALIDATED" parenthetical), so this is squarely in the change set, not standing text.

- §3.4 operative rule, current 439-445: `VAR` is "either one of the catalog's documented vendor configuration-home variables (`CLAUDE_CONFIG_DIR`, `CODEX_HOME` — applied exactly like the catalogued pointer) OR a name matching `[A-Z][A-Z0-9_]*` that is NOT in `_RESERVED_ENV` (2.12) and NOT a sandbox variable". Two branches; the first admits two `_RESERVED_ENV` keys.
- Test matrix, current 750: `seats add --config-home` "accepts `CLAUDE_CONFIG_DIR=.claude`". Acceptance is REQUIRED by the TDD bullet.
- I6, current 338-340: "VALIDATED (an operator-declared variable can never name a controller-owned or sandbox variable ...)". The plan's own 2.12 (current 265) titles `controller.py:38-61` the "**Controller-owned environment guard**", and `controller.py:39-40` makes `CLAUDE_CONFIG_DIR` and `CODEX_HOME` its first two entries. So I6, in the plan's own vocabulary, is falsified by the plan's own required test case.
- Slice C step 5, current 695-697: "`add_seat` ... validates `--config-home` per 3.4 (variable name against `_RESERVED_ENV` and the sandbox set, directory relative / no `..` / beneath the home)". Read literally this is the second branch only, and an implementer building step 5 as written writes a validator that REFUSES `CLAUDE_CONFIG_DIR=.claude` and fails the test at line 750. This is why it blocks rather than reads as a compression: step 5 is the implementation instruction, and it and the test bullet cannot both be satisfied. In my sealed pass I wrote "nothing builds wrong" on the strength of the "per 3.4" delegation; the parenthetical that follows the delegation restates the rule incorrectly, and the restatement is what an implementer codes.

Fix is one clause at each site and changes no behaviour: I6 → "can never name a sandbox variable, or a controller-owned variable other than the vendor's own documented configuration home"; step 5 → "(variable name is a documented vendor configuration-home variable, or is outside `_RESERVED_ENV` and the sandbox set; directory relative / no `..` / beneath the home)".

Per the docket's "name EVERY blocking finding" clause: this is the only blocking finding I can establish in this pass. Everything else below is non-blocking and recorded so a second pass by me finds nothing new.

## Where MSG-28 overreaches

MSG-28 also calls this a failure of criterion 3 ("the declared pointer unable to reach a controller-owned or sandbox variable"). On fresh evidence that part does not hold, and I record it so the fold is not over-scoped:

- `controller.py:682-706` (`_adapter_environment`) writes HOME, XDG_CONFIG_HOME/CACHE_HOME/DATA_HOME (694-696), TMPDIR/TEMP/TMP, GIT_CEILING_DIRECTORIES, PYTHONDONTWRITEBYTECODE, PYTEST_DISABLE_PLUGIN_AUTOLOAD, PYTEST_ADDOPTS. It writes NEITHER `CLAUDE_CONFIG_DIR` NOR `CODEX_HOME` (grep over the whole export returns only `controller.py:39-40` and `tests/test_controller.py:637`). So an accepted `CLAUDE_CONFIG_DIR=.claude` replaces nothing the controller sandboxes — the round-5 falsification ("would replace a controller-sandboxed variable one process below the profile guard") is genuinely repaired, and the "What stays sandboxed for the seat: HOME, XDG_*, TMPDIR, build paths, the read-only source export, the detached stdin — and a declared pointer can name none of these" sentence (current 498-500) is TRUE of every item it enumerates.
- The accepted branch grants exactly the reach the CATALOGUED pointer already grants unconditionally (§3.4 current 481-484: `CLAUDE_CONFIG_DIR` → `<real home>/.claude` for catalog seats, no operator declaration involved), which §3.4's sandbox-boundary paragraph argues at length and which the docket's own fold list blesses by name (docket lines 50-52). Reading criterion 3 to forbid it would put the docket's criteria in conflict with the docket's fold list; the docket's tie-break ("the reading that serves this goal prevails") resolves that toward the fold list. The defect is that I6 and step 5 describe the blessed rule wrongly, not that the rule is unsafe.

Criterion 1 (the one new citation) holds, established fresh: `src/debate/seats.py:482-487` is a `for part in argv:` loop raising `refused: command looks credential-shaped; credentials belong in a self-sourcing wrapper, never the registry` — the plan quotes it verbatim at current 487-489 and now attributes it to the registry rather than the catalog notes. MSG-20 obs 3 resolved.

## True change set (recomputed, not taken from the fold list)

My sandbox again refused `diff` and refused `ls` of the docket tree (Bash is denied outside the test allowlist), so per the docket's tooling note I read both versions and compared section by section, anchored on line-count offsets at every section and bullet boundary. The ledger closes exactly at +43 (976 → 1019): +14 new `Amended:` block (82-94), +2 I6, +1 I10, +13 §3.4 "Known flags or refusal", +2 §3.4 sandbox-boundary bullet, +2 Slice C step 5, +2 bridge test bullet, +5 open/admission test bullet, +2 pairing test bullet; every section header lands at its predicted line (round-5 152/291/391/517/549/586/610/628/753/863/888/904/927/951 → current 166/305/408/549/581/618/642/660/796/906/931/947/970/994). Two in-place zero-delta edits: the wrapper-credential sentence (fold 4) and the E2E fixture bullet (fold 2). Items: (1) sixth `Amended:` block; (2) I6 "VALIDATED"; (3) I10 widened to either-empty list plus failed pointer; (4) §3.4 pointer validation 437-450; (5) §3.4 wrapper sentence now citing `seats.py:483-487`; (6) "what stays sandboxed" gains "a declared pointer can name none of these"; (7) Slice C step 5 validation clause; (8) bridge exit-2 test bullet 733-736; (9) open/admission refusal-acceptance matrix 747-753; (10) pairing test names all four optional fields; (11) E2E fixture carries a `config_home`. No artifact edit is absent from the fold list; the docket instruction sheet is excluded scaffolding by name.

## (a)/(b) Fold-to-finding mapping

Every round-5 finding has a fold, and each fold's substance lands: blocker → items 2,3,4,6,7,8,9 (repaired by construction as argued above — HOME, every XDG the controller derives, TMPDIR, PATH, PYTHONPATH and every other `_RESERVED_ENV` key are refused, `<dir>` cannot be absolute or contain `..`, and negative tests now exist at all three enforcement layers); MSG-20 obs 1 → item 11 (the E2E `configuration-home: operator (...)` assertion at 781-782 is now sourced); obs 2 → item 3; obs 3 → item 5; obs 4 → item 10; bookkeeping → item 1. The blocking finding above is not a missing fold — it is a fold whose new text contradicts §3.4 and the tests.

## (c) Coherence sweep, standing criteria

Criterion 2 fails as stated above; the rest of the sweep is clean: the three enforcement points (`seats add` / `_brokered_adapter` at open / bridge at invocation) each have a matching test bullet, and I10 matches §3.4's belt-and-braces conditions. Criterion 3 holds on the substance: PROTOCOL.md §5 re-read in the docket copy at 115-126 ("Settings sources, hooks, plugins, MCPs and session persistence are off.") with no exception path and no waiver; `controller.py:38-61` is unmodified in the export and the plan pins it unchanged (§5, §6, O19). Criteria implicated by the change set are established fresh above; unimplicated criteria stand by citation to MSG-22 (and MSG-16/12/8 where MSG-22 cited them): round-4 F1/F2 and both observations, all of section 2 (round-5 152-289 → current 166-303, zero delta), I2, 2.13, §6, O12, criterion 3's §5-no-exception-path finding, criterion 5 (§7 unchanged, round-5 904-925 → current 947-968, zero delta). I did not vote on the owner's field observations, the host CLI flags at the end of section 2, or the deferred field-pass measurements.

## Non-blocking observations

1. `PYTEST_DISABLE_PLUGIN_AUTOLOAD` (`controller.py:702`) is the only variable `_adapter_environment` writes that is in neither `_RESERVED_ENV` nor the plan's sandbox set, so it would validate as a pointer. Not a hole (it is outside the profile-level guard too, so no asymmetry with hand-authored adapters, and a directory path is still truthy so autoload stays off), but the plan's stated principle is "the engine itself writes it into the child environment", and the sandbox set is one name short of that set. One-word fix while the blocker is folded.
2. "XDG_*" in the sandbox sentence is shorthand for exactly the three names at `controller.py:694-696`; an XDG name the controller never derives (`XDG_STATE_HOME`) validates as an ordinary new variable beneath the operator's home, replacing nothing sandboxed.
3. `DEBATE_BRIDGE_REAL_HOME` is in neither exclusion set and could be declared. Harmless: the bridge validates against the value in its OWN environment before composing the child environment, so the override reaches only the seat and still points beneath the operator's home.
4. "ADMISSION IS THE FLAGS RULE ALONE" / "The configuration home is NOT an admission condition" (current 452-458) now coexists with `_brokered_adapter` refusing an invalid stored pointer at open. The defensible reading is that the pointer's ABSENCE never blocks admission (the round-4 fold) while a malformed stored value is an integrity refusal; one clarifying clause would settle it.

## Bar

The command passes and every round-5 finding is resolved by a fold in the true change set, but criterion 2 fails: I6 (current 338-340) and Slice C step 5 (current 695-697) state the pointer validation in terms that the operative rule at 439-445 and the required acceptance test at 750 contradict, and step 5 is the directive an implementer builds from. NO_PASS.

Controller-Decision:
- decision: NO_PASS

Controller-Provenance:
- phase: deliberation
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: 71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d
- controller-config-sha256: 03233f08f964b9e17511b194abf62486080923e33748d44388df45360f942bdb
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: 55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df
- docket-revision-sha256: 76277921029d85bcfb008ebbdd7194e82baf7cd2a2bfefb41a8f8bbbcb5a2c22
- input-sha256: b0716b7c8219c1ea1c1bd3c3ae425ef83cbe0e5d712cd678f956b7e0a24d0398
- requested-model: claude-opus-5
- runtime-model: claude-opus-5
- reasoning-effort: high
- cli-version: claude-code 2.1.235
- isolation-mode: advisory

## MSG-30 | 2026-08-20T21:32:53+00:00 | from: owner | type: close | thread: plan-v080-part2-8 | refs: -

Controller closed the managed case as NO_PASS. Reason: party-vote-agreement. Supervisor messages were not counted as party votes.

Controller-Terminal:
- terminal-result: NO_PASS
- close-reason: party-vote-agreement

## MSG-31 | 2026-08-20T21:35:13+00:00 | from: owner | type: review-request | thread: plan-v080-part2-9 | refs: feature/installation-onboarding-v080@36852335ff92d8c97db83721bfa8e99741ed1cfe

Fix-report and round-7 review request (fold-delta) for the v0.8.0 part-2 plan doc `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md`. Round 6 (case `plan-v080-part2-8`) closed NO_PASS at MSG-30: opus sealed PASS (MSG-27), codex NO_PASS (MSG-28) on I6/step-5 wording that contradicted 3.4's accepted `CLAUDE_CONFIG_DIR` branch, opus concurred on coherence only (MSG-29). Every finding has been folded; the docket restates each because the channel record is outside the seats' docket:

1. I6 and Slice C step 5 now state the pointer rule with the same two branches as 3.4 (the vendor's own documented configuration-home variable, or a name outside `_RESERVED_ENV` and the sandbox set). No behaviour change; the test matrix is unchanged.
2. Observations folded: the sandbox set is defined as every name `_adapter_environment` writes (now including `PYTEST_DISABLE_PLUGIN_AUTOLOAD`) plus `PATH`, `PYTHONPATH`, `DEBATE_BRIDGE_REAL_HOME`; "not an admission condition" is clarified (absence never blocks; a malformed stored pointer is a registry-integrity refusal at open).

The round-6 artifact version is materialized in the docket as `plan-round6.md`; compute the true change set yourself per the docket's fold-delta clause. Verdicts cite your own fresh evidence; standing round-6 criteria may be cited by MSG id where your diff and sweep leave them unimplicated.


Controller-Docket-Provenance:
- topology: recommended-three-agent
- controller-config-sha256: 7375798bdf63aaea85343eb7b80906d46055e6b41f5130afbff8147881fbed14
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- docket-revision-sha256: c3ed8d2a0eab6fb9107db8b8424160c2c643d42f5ff0c7db97a9e00d493865b6
- docket-files: [{"path": "collab/PROTOCOL.md", "sha256": "dfb3950a24d38ac308632baa117e961ae9da00ed1b8aac8a48261d8cbeaeb9c6", "tracked_at_source_ref": true}, {"path": "collab/plan-v080-part2-63227.debate.json", "sha256": "24740b4f553046288ff8d69a939e8bde3959b09183bc06c34addb936440e7a49", "tracked_at_source_ref": false}, {"path": "docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md", "sha256": "e51e1a5282f1ee71496b9be0912423404b0649765e6509c75b589360cdd3af9d", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-round6.md", "sha256": "075c15971b48aea10bfc593fe8f0cfec7c7e6d468e7b7982c759f21b259dcbde", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-docket-r7.md", "sha256": "306e6e7ab374aa0d6d852d5aae46627cb8400408e521289573ef98a50773c8ca", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat-result.schema.json", "sha256": "0f03c5e1f0be74c320750c3c2baf5a5a14c39622e34babe8c0b30781578161b1", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat_adapter.py", "sha256": "5e3f30bd1d0e3dd0b2ff605bc3b4412970912744c8456f944af6128ad46e71a6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/watcher-r7.json", "sha256": "7375798bdf63aaea85343eb7b80906d46055e6b41f5130afbff8147881fbed14", "tracked_at_source_ref": false}]
- profile-sha256: {"codex": "c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8", "opus": "71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d"}
- sanitized-profile-manifests: {"codex": {"authentication_mode": "Codex subscription OAuth state exposed only to the local wrapper; user config and rules ignored", "author_relationship": "author-independent", "cli_version": "codex-cli 0.148.0", "command_sha256": "bc02f45ab947a2ea025ac951b33fdc4235b974411683e1505590178a869f8667", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "gpt-5.6-terra", "isolation_mode": "advisory", "party": "codex", "permission_policy": "read-only source export at filesystem layer; workspace-write sandbox limited to controller invocation output/build path", "provider": "openai", "reasoning_effort": "high", "requested_model": "gpt-5.6-terra", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}, "opus": {"authentication_mode": "subscription OAuth state exposed only to the local wrapper; safe mode and settings sources disabled", "author_relationship": "author-independent", "cli_version": "claude-code 2.1.235", "command_sha256": "2107038579b81e9434ce9eb3b5f185939951bc78b3a675d4a1e8435a55f7fa27", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "claude-opus-5", "isolation_mode": "advisory", "party": "opus", "permission_policy": "read-only source export; result path controller-owned; Read/Grep/Glob plus test-only Bash allowlist", "provider": "anthropic", "reasoning_effort": "high", "requested_model": "claude-opus-5", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}}
- source-manifest-sha256: {"codex": "bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3", "opus": "55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df"}

## MSG-32 | 2026-08-20T21:42:55+00:00 | from: opus | type: verdict | thread: plan-v080-part2-9 | refs: -

VERDICT: NO_PASS — one blocking finding under criterion 2. Criterion 6 passes; criteria 1 and 3 hold on fresh evidence; 4 and 5 stand by citation.

## Criterion 6 — the exact command

Ran literally, no prefixes/suffixes/pipes:

`python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py`

Result: `111 passed in 8.33s` (72 dots then 39 dots, 100%). Criterion 6 SATISFIED.

## Method note (docket tooling clause)

My sandbox refused `diff` ("Permission to use Bash has been denied … don't ask mode" on `diff -u <round6> <current>`), so per the docket's tooling note I read both versions in full and compared section by section: `plan-round6.md` (1019 lines) and `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md` (1038 lines). The same denial blocked `ls`/`cat`; I used the file tools. The pytest command itself ran.

## True change set (computed, not taken from the fold list)

Five edits, each located by section-by-section comparison and cross-checked by line arithmetic (1038 − 1019 = 19 = 10 + 1 + 3 + 3 + 2, and every offset below is confirmed by matching anchor lines):

1. New round-6 `Amended:` block, current 96-104 plus its blank line (+10). Round6 96 "Target release" → current 106.
2. I6: round6 339-340 "can never name a controller-owned or sandbox variable" → current 349-351 "can never name a sandbox variable, nor a controller-owned variable other than the vendor's own documented configuration home" (+1). Anchor: I7 round6 342 → current 353.
3. 3.4 sandbox-set definition: round6 442-444 (`HOME`, three `XDG_*`, `TMPDIR`/`TEMP`/`TMP`, `PATH`, `PYTHONPATH`) → current 453-458 ("every name `_adapter_environment` writes (`controller.py:682-706`: …) plus `PATH`, `PYTHONPATH`, `DEBATE_BRIDGE_REAL_HOME`") (+3). Anchor: round6 445 → current 459.
4. 3.4 admission clause: round6 458 "(next bullet). The doctor" → current 472-475 "(next bullet): its ABSENCE never blocks a seat; a STORED pointer that fails validation is refused at open as a registry-integrity failure, which a registry written by `seats add` can never carry." (+3). Anchor: round6 459 → current 476.
5. Slice C step 5: round6 696-697 "(variable name against `_RESERVED_ENV` and the sandbox set, directory relative / no `..` / beneath the home)" → current 713-716 "(the variable name is a documented vendor configuration-home variable — `CLAUDE_CONFIG_DIR`, `CODEX_HOME` — or is outside `_RESERVED_ENV` and the sandbox set; the directory is relative, has no `..`, and resolves beneath the home)" (+2). Total 1038 ✓.

No artifact edit outside the author's fold list was found. Sections 1, 2, 3.2, 3.3, 3.5-3.8, Slices A/B/D, 5-9 read identical line for line. (Line arithmetic alone cannot catch an equal-length substitution; I read both documents in full and found none.)

## (a) Each fold resolves its round-6 finding — yes, at the three sites it touches

Fold 1 (I6 + step 5) states 3.4's two branches. Fold 2a's enumeration is exact against the export (below). Fold 2b's clause is coherent with I10 (367), 3.4's open-time re-check (461-463) and O1 (993): absence never blocks; a malformed STORED value is an integrity refusal — the two are distinct conditions, no contradiction.

## (b) Reverse check — every round-6 finding has a fold

Blocker (I6 / step 5 wording): folds 1a/1b. `PYTEST_DISABLE_PLUGIN_AUTOLOAD` missing from both exclusion sets: fold 2a adds it. `XDG_*` shorthand: 3.4 enumerates the three derived names. `DEBATE_BRIDGE_REAL_HOME` declarable: fold 2a puts it in the sandbox set, so it can no longer be declared. "Not an admission condition" clarifying clause: fold 2b. Bookkeeping: fold 3, provenance matches (MSG-27/29 opus, MSG-28 codex, MSG-30 close). No round-6 finding is without a fold.

## (c) Coherence sweep — BLOCKING FINDING (criterion 2)

The docket requires that "3.4, I6, I10, Slice C step 5 **and the test matrix** now state ONE rule with the same two branches everywhere". They do not. The pointer rule survives at a FOURTH restatement site that the fold did not touch, in the same one-branch form that was ruled blocking in round 6.

Slice C tests, bridge bullet, current lines 752-754 (unchanged from round6 733-735):

> "the bridge exits 2 without a seat call when its pointer argument **names a reserved or sandbox variable** or a directory outside `DEBATE_BRIDGE_REAL_HOME`"

"Reserved" is unambiguous in this plan's own vocabulary: 2.12 (line 276) says "`_RESERVED_ENV` names `CLAUDE_CONFIG_DIR` and `CODEX_HOME` first", and I confirmed it fresh — `controller.py:38-61` in the pinned export begins `_RESERVED_ENV = {"CLAUDE_CONFIG_DIR", "CODEX_HOME", "HOME", …}`. The plan uses the word precisely elsewhere ("(none reserved)", 433; "a path, not reserved", 493). So this test spec directs an implementer to make the bridge exit 2 for `CLAUDE_CONFIG_DIR` — which contradicts:

- 3.4's operative rule, 450-452: `VAR` may be "one of the catalog's documented vendor configuration-home variables (`CLAUDE_CONFIG_DIR`, `CODEX_HOME` — applied exactly like the catalogued pointer)";
- 3.4's sandbox-boundary bullet, 498-500: the bridge "re-introduces the vendor's configuration home through the vendor's documented variable (`CLAUDE_CONFIG_DIR` -> `<real home>/.claude`, `CODEX_HOME` -> `<real home>/.codex`)";
- the folded I6 (349-351) and the folded step 5 (713-716);
- the `seats add` test line 769-770, which explicitly "accepts `CLAUDE_CONFIG_DIR=.claude`";
- **the immediately preceding clause of the very same test bullet**, 749-751: "env passed to the seat carries the config-home variable for claude/codex". One bullet asserts the bridge passes `CLAUDE_CONFIG_DIR` for a Claude seat and, in the next breath, that it exits 2 without a seat call when its pointer argument names a reserved variable;
- the E2E test, 798-802, which requires a terminal PASS with `configuration-home: operator (...)`.

This is the round-6 defect class exactly — a gloss of the pointer rule omitting the vendor-documented branch, in a build directive an implementer codes from (here, a TDD test spec, which the plan's own process makes the first artifact written: "Each slice: tests first (TDD)", 681-682). The author's fold list item 1 asserts "the test matrix is unchanged"; that is true as a change inventory and my diff confirms it, but criterion 2 requires the test matrix to state the same rule, and this line does not. Fixing it costs one clause, e.g.: "…when its pointer argument names a variable that fails the 3.4 rule (a name in `_RESERVED_ENV` other than a documented vendor configuration home, or a sandbox-set name) or a directory outside `DEBATE_BRIDGE_REAL_HOME`".

No other contradiction found. I swept every occurrence of "reserved / `_RESERVED_ENV` / sandbox set / sandbox variable / config-home / configuration home / `CLAUDE_CONFIG_DIR`" in the current artifact (39 hits). The only other one-branch phrasing is the round-5 `Amended:` line at 83-86, which is superseded on its face by the round-6 `Amended:` line at 99-102 that redefines the sandbox set, and which carries both branches anyway — historical changelog text, not a live rule statement, and not a finding.

## Criterion 1 — section-2 evidence (new citation established fresh)

`controller.py:682-706` in the pinned export: `_adapter_environment` writes exactly eleven names in its `environment.update({...})` at 691-704 — `HOME` (693), `XDG_CONFIG_HOME` (694), `XDG_CACHE_HOME` (695), `XDG_DATA_HOME` (696), `TMPDIR` (697), `TEMP` (698), `TMP` (699), `GIT_CEILING_DIRECTORIES` (700), `PYTHONDONTWRITEBYTECODE` (701), `PYTEST_DISABLE_PLUGIN_AUTOLOAD` (702), `PYTEST_ADDOPTS` (703). That is an exact, complete match for the eleven names 3.4 enumerates at 454-457, in the same order. It writes neither `CLAUDE_CONFIG_DIR` nor `CODEX_HOME` (independently re-confirming MSG-29). `PATH`/`PYTHONPATH` are not written here, which is why 3.4's "plus `PATH`, `PYTHONPATH`, `DEBATE_BRIDGE_REAL_HOME`" is the correct construction. Criterion 1 HOLDS; the rest of section 2 is unimplicated and stands by citation to MSG-29 (and MSG-22/16/12/8 where it cited them).

## Criterion 3 — safety and trust invariants: HOLDS (implicated by folds 1a/2a, established fresh)

`collab/PROTOCOL.md:115-126` read fresh: §5 states "Settings sources, hooks, plugins, MCPs and session persistence are off." with no exception or waiver path, and 123-126 gives the advisory-mode caveat — 2.13 (282-293) reports both accurately. The profile-level guard is unchanged (3.4 at 490-497; §6 at 956; §5 files list at 934). Because the sandbox set is now defined as the exact write set of `_adapter_environment` plus `PATH`/`PYTHONPATH`/`DEBATE_BRIDGE_REAL_HOME`, and I verified that write set exhaustively above, a declared pointer provably cannot replace any variable the controller sandboxes; the vendor branch (`CLAUDE_CONFIG_DIR`, `CODEX_HOME`) replaces nothing sandboxed and grants exactly the reach the catalogued pointer already grants. The directory rule (relative, no `..`, beneath the operator's home, re-checked at `seats add`, at open, and by the bridge against `DEBATE_BRIDGE_REAL_HOME`) keeps the pointer inside the operator's home. The finding above is a wording/coherence defect, not a safety one — I do not raise it under criterion 3.

## Criteria 4 and 5 — unimplicated by the true change set or the sweep; stand by citation to MSG-29 (which cited MSG-22/16/12/8). Per the docket I did not vote on the owner's field observations, the host CLI flags at the end of section 2, or the deferred field-pass measurements.

## Completeness

This is the only blocking finding I can establish in this pass. A second pass over the same artifact would find nothing new: the pointer rule now appears at four live restatement sites (3.4 449-459, I6 349-351, step 5 713-716, and the test matrix at 752-754 and 767-770); three state both branches, and 752-754 is the single one that does not. PASS is one clause away.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: be25503da962162899c62d965dcc3ffbf01bddd7ab1f904514ae75f2457824b8
- phase: sealed
- captured-at: 2026-08-20T21:39:54+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: 71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d
- controller-config-sha256: 7375798bdf63aaea85343eb7b80906d46055e6b41f5130afbff8147881fbed14
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: 55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df
- docket-revision-sha256: c3ed8d2a0eab6fb9107db8b8424160c2c643d42f5ff0c7db97a9e00d493865b6
- input-sha256: f825307d04c47d942b018f78af36a924315ac47ca1e70755cdb5cc10d0c2e8ea
- requested-model: claude-opus-5
- runtime-model: claude-opus-5
- reasoning-effort: high
- cli-version: claude-code 2.1.235
- isolation-mode: advisory

## MSG-33 | 2026-08-20T21:42:55+00:00 | from: codex | type: verdict | thread: plan-v080-part2-9 | refs: -

The required command passed: `python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py` → `111 passed in 8.85s`. Blocking findings remain: (1) criterion 2 is internally contradictory. Section 3.4 and Slice C step 5 allow the documented-variable branch (`CLAUDE_CONFIG_DIR`/`CODEX_HOME`), but the bridge test matrix requires exit 2 whenever its pointer names a reserved variable. The source command `nl -ba src/debate/controller.py | sed -n '30,70p'` shows both names are in `_RESERVED_ENV`; thus the allowed branch is also required to be rejected. (2) criterion 1 is contradicted by the pinned export: `test -e /home/zoltan/Projects/debate/var/debate/plan-v080-part2-63227/controller/exports/36852335ff92d8c97db83721bfa8e99741ed1cfe/codex/collab/PROTOCOL.md; printf 'source_collab_protocol_exit=%s\n' "$?"` → `source_collab_protocol_exit=1`. Section 2.13 claims that exact source-export path and bases the §5 invariant evidence on it, but it is absent; the export instead has a different root `PROTOCOL.md` template.

Controller-Decision:
- decision: NO_PASS

Controller-Sealed-Reveal:
- reveal-id: be25503da962162899c62d965dcc3ffbf01bddd7ab1f904514ae75f2457824b8
- phase: sealed
- captured-at: 2026-08-20T21:42:55+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8
- controller-config-sha256: 7375798bdf63aaea85343eb7b80906d46055e6b41f5130afbff8147881fbed14
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3
- docket-revision-sha256: c3ed8d2a0eab6fb9107db8b8424160c2c643d42f5ff0c7db97a9e00d493865b6
- input-sha256: 66e2a4185986df9bdb57974c8ab9132e6b7bc154080983cfd493d8eb50f03606
- requested-model: gpt-5.6-terra
- runtime-model: gpt-5.6-terra
- reasoning-effort: high
- cli-version: codex-cli 0.148.0
- isolation-mode: advisory

## MSG-34 | 2026-08-20T21:42:55+00:00 | from: owner | type: close | thread: plan-v080-part2-9 | refs: -

Controller closed the managed case as NO_PASS. Reason: party-vote-agreement. Supervisor messages were not counted as party votes.

Controller-Terminal:
- terminal-result: NO_PASS
- close-reason: party-vote-agreement

## MSG-35 | 2026-08-20T21:44:28+00:00 | from: owner | type: review-request | thread: plan-v080-part2-10 | refs: feature/installation-onboarding-v080@36852335ff92d8c97db83721bfa8e99741ed1cfe

Fix-report and round-8 review request (fold-delta) for the v0.8.0 part-2 plan doc `docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md`. Round 7 (case `plan-v080-part2-9`) closed NO_PASS at MSG-34 on MSG-32 (opus) and MSG-33 (codex 1-2). Both findings are folded; the docket restates each because the channel record is outside the seats' docket:

1. The last one-branch restatement of the pointer rule — the Slice C bridge test bullet — now states the 3.4 rule (a `_RESERVED_ENV` name other than a documented vendor configuration home, a sandbox-set name, or a directory outside the operator's home fails; `CLAUDE_CONFIG_DIR=.claude` runs). No behaviour change.
2. 2.13 and the section-2 preamble now say where `collab/PROTOCOL.md` is checkable: tracked at the pinned ref, excluded from the source export by the controller's policy (`controller.py:477-478`, `578`), materialized in the docket with `tracked_at_source_ref: true` (`controller.py:629`).

The round-7 artifact version is materialized in the docket as `plan-round7.md`; compute the true change set yourself per the docket's fold-delta clause. Verdicts cite your own fresh evidence; standing round-7 criteria may be cited by MSG id where your diff and sweep leave them unimplicated.


Controller-Docket-Provenance:
- topology: recommended-three-agent
- controller-config-sha256: eb4496e756e7655abe6f8294f95458f7c259d828b28366f7a0f37dd0b9b1a2c3
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- docket-revision-sha256: 1c5468cbf8a2fe591dc7084836c2acf71660858d4fc8ee3395e2348ccee7e2cc
- docket-files: [{"path": "collab/PROTOCOL.md", "sha256": "dfb3950a24d38ac308632baa117e961ae9da00ed1b8aac8a48261d8cbeaeb9c6", "tracked_at_source_ref": true}, {"path": "collab/plan-v080-part2-63227.debate.json", "sha256": "24740b4f553046288ff8d69a939e8bde3959b09183bc06c34addb936440e7a49", "tracked_at_source_ref": false}, {"path": "docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md", "sha256": "d7bb9532b1ccbe1d581cc46a1de301813bf5f7e6a19695a88a11c99324f29a7e", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-round7.md", "sha256": "e51e1a5282f1ee71496b9be0912423404b0649765e6509c75b589360cdd3af9d", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/plan-docket-r8.md", "sha256": "ed330362b6e99bc77f90a97b3f1b3d0d9af30f50ff39e9718de325397d88b69a", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat-result.schema.json", "sha256": "0f03c5e1f0be74c320750c3c2baf5a5a14c39622e34babe8c0b30781578161b1", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/seat_adapter.py", "sha256": "5e3f30bd1d0e3dd0b2ff605bc3b4412970912744c8456f944af6128ad46e71a6", "tracked_at_source_ref": false}, {"path": "var/debate/plan-v080-part2-63227/watcher-r8.json", "sha256": "eb4496e756e7655abe6f8294f95458f7c259d828b28366f7a0f37dd0b9b1a2c3", "tracked_at_source_ref": false}]
- profile-sha256: {"codex": "c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8", "opus": "71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d"}
- sanitized-profile-manifests: {"codex": {"authentication_mode": "Codex subscription OAuth state exposed only to the local wrapper; user config and rules ignored", "author_relationship": "author-independent", "cli_version": "codex-cli 0.148.0", "command_sha256": "bc02f45ab947a2ea025ac951b33fdc4235b974411683e1505590178a869f8667", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "gpt-5.6-terra", "isolation_mode": "advisory", "party": "codex", "permission_policy": "read-only source export at filesystem layer; workspace-write sandbox limited to controller invocation output/build path", "provider": "openai", "reasoning_effort": "high", "requested_model": "gpt-5.6-terra", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}, "opus": {"authentication_mode": "subscription OAuth state exposed only to the local wrapper; safe mode and settings sources disabled", "author_relationship": "author-independent", "cli_version": "claude-code 2.1.235", "command_sha256": "2107038579b81e9434ce9eb3b5f185939951bc78b3a675d4a1e8435a55f7fa27", "cost_mode": "subscription", "environment_additions": {"PYTHONPATH": "25a6634263c1b1f6fc4697a04e2b9904ea4b042a89af59dc93ec1f5d44848a26"}, "environment_allowlist": ["PATH", "LANG", "LC_ALL", "SSL_CERT_FILE", "SSL_CERT_DIR", "HTTP_PROXY", "HTTPS_PROXY", "ALL_PROXY", "NO_PROXY"], "expected_runtime_model": "claude-opus-5", "isolation_mode": "advisory", "party": "opus", "permission_policy": "read-only source export; result path controller-owned; Read/Grep/Glob plus test-only Bash allowlist", "provider": "anthropic", "reasoning_effort": "high", "requested_model": "claude-opus-5", "result_schema_version": 1, "retry_limit": 1, "schema_version": 1, "session_persistence": false, "settings_sources": [], "timeout_seconds": 1200}}
- source-manifest-sha256: {"codex": "bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3", "opus": "55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df"}

## MSG-36 | 2026-08-20T21:51:30+00:00 | from: opus | type: verdict | thread: plan-v080-part2-10 | refs: -

## Verification run (criterion 6)

Command, run literally in the export root `/home/zoltan/Projects/debate/var/debate/plan-v080-part2-63227/controller/exports/36852335ff92d8c97db83721bfa8e99741ed1cfe/opus`:

`python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py`

Result: `111 passed in 12.11s` (no failures, no errors, no skips reported).

## Tooling note (as the docket requires)

My sandbox refused `diff`: `diff -u <docket>/var/debate/plan-v080-part2-63227/plan-round7.md <docket>/docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md` was denied ("Permission to use Bash has been denied"), as was `ls` under the docket root. I therefore read BOTH versions in full (round-7: lines 1-1038; current: lines 1-1058) and compared section by section, cross-checking with anchored line-number greps in both files so every structural offset is accounted for.

## True change set (computed, not taken from the fold list)

Anchor offsets from `^(## |### |Amended: |Target release)` greps on both files: §1 141→150 (+9), §2 176→185 (+9), §3 315→332 (+17), Slice C 685→702 (+17), Slice A 815→835 (+20), and +20 for every heading through §9 (1013→1033). Interior anchors: "call when either list is empty" 749→766 (+17), "pointer argument" 752→769 (+17), "monkeypatched `open`/`Path.read_*`" 755→775 (+20). Every offset is fully explained by exactly four edits, and the section-by-section read found no other differing text:

1. New eighth `Amended:` paragraph, current 106-113 (+9) — bookkeeping fold 3.
2. §2 heading "checkable in the export at `3685233`" → "checkable at `3685233`" (185) and the rewritten preamble 187-195 (+6) — fold 2.
3. 2.13 gains the inline location note, current 307-310 (+2) — fold 2.
4. Slice C bridge test bullet: round-7 752-754 → current 769-774 (+3) — fold 1.

Fold list ↔ change set match in both directions: no artifact edit is missing from the fold list, and no fold-list item is absent from the artifact.

## (a) Each fold resolves its round-7 finding

- **Finding 1 (MSG-32 / MSG-33 #1)** — resolved. The bullet now reads "the bridge exits 2 without a seat call when its pointer argument fails the 3.4 rule — a name in `_RESERVED_ENV` other than a documented vendor configuration home (`CLAUDE_CONFIG_DIR`, `CODEX_HOME`), a sandbox-set name, or a directory outside `DEBATE_BRIDGE_REAL_HOME` — and runs normally with `CLAUDE_CONFIG_DIR=.claude`" (769-774). The one-branch wording is gone and the affirmative `CLAUDE_CONFIG_DIR=.claude` case is now asserted, so the test spec matches 3.4, I6, I10, Slice C step 5, the `seats add` matrix and the E2E bullet.
- **Finding 2 (MSG-33 #2)** — resolved. `collab/PROTOCOL.md` is no longer placed in the source export: the preamble (187-195) and 2.13 (307-310) both say it is tracked at the pinned ref, excluded from the export by policy, and reaches seats as the docket's copy.

## (b) Reverse check

Both round-7 findings have a corresponding fold; no round-7 finding is left without one.

## (c) Criterion 1 — the three new citations and the docket record, established fresh

- `controller.py:477-478` — `def _is_separated(path)` returns `path.parts[0] in ("collab", "var", ".git")`; it gates export extraction at 565-567 (`if _is_separated(relative): excluded.append(...); continue`). ✓
- `controller.py:578` — manifest key `"exclusion_policy": ["collab/", "var/", ".git/"]`. ✓
- `controller.py:629` — docket record field `"tracked_at_source_ref": tracked`, where `tracked = pinned is not None` from `_tracked_bytes(repo, config.source_ref, relative)` (617-618). ✓
- This case's docket manifest (`controller/dockets/1c5468cb…/manifest.json`) records `{"path": "collab/PROTOCOL.md", "sha256": "dfb3950a…", "tracked_at_source_ref": true}`. ✓
- The export root listing contains no `collab/` directory (top-level `PROTOCOL.md` in the export is a different document — a grep for "Isolation and authority" in it returns no matches), and the docket copy `collab/PROTOCOL.md:115-126` carries §5 "Isolation and authority" with "Settings sources, hooks, plugins, MCPs and session persistence are off." at 119 and the advisory paragraph at 123-126, exactly as 2.13 cites. ✓

## Criterion 2 — coherence sweep, in particular every live restatement of the pointer rule

3.4 (VAR = documented vendor home `CLAUDE_CONFIG_DIR`/`CODEX_HOME`, or `[A-Z][A-Z0-9_]*` outside `_RESERVED_ENV` and the sandbox set; dir relative, no `..`, beneath the operator's home), I6, I10 (defers to "the 3.4 validation"), Slice C step 5, the rewritten bridge test bullet, the `seats add --config-home` accept/refuse matrix (refuses `HOME`, `XDG_CONFIG_HOME`, `TMPDIR`, `PATH`, `GIT_DIR`, lowercase/malformed, absolute, `..`; accepts `CLAUDE_CONFIG_DIR=.claude`, `MYTOOL_HOME=.mytool`) and the E2E bullet now all state the same two branches. The two premises the new bullet depends on I established fresh rather than by citation: `controller.py:38-61` lists `CLAUDE_CONFIG_DIR` and `CODEX_HOME` first in `_RESERVED_ENV`, and `_adapter_environment` (`controller.py:682-706`) writes exactly the eleven names 3.4 enumerates (`HOME`, `XDG_CONFIG_HOME`, `XDG_CACHE_HOME`, `XDG_DATA_HOME`, `TMPDIR`, `TEMP`, `TMP`, `GIT_CEILING_DIRECTORIES`, `PYTHONDONTWRITEBYTECODE`, `PYTEST_DISABLE_PLUGIN_AUTOLOAD`, `PYTEST_ADDOPTS`) and neither vendor variable. No change contradicts any other part of the artifact: the §2 heading/preamble/2.13 edits are consistent with §5's "No change to … `collab/PROTOCOL.md`", the new `Amended:` line describes exactly the four edits I found and its "No behaviour change" is accurate (3.4 is untouched), and nothing in §§3-9 moved.

## Standing criteria not implicated by the change set or the sweep

Cited to MSG-32 (which cited MSG-29/22/16/12/8 where relevant): all of section 2 except the preamble and 2.13's location note; the absolute statements I2; 2.13's substance; §6; O12; criteria 4 (honesty of capability claims) and 5 (gate/lifecycle compliance). Criterion 3 (safety/trust invariants complete and non-contradictory) is implicated only through the pointer-rule restatements, which I re-established above; I1-I10 remain complete and mutually consistent, with no operator waiver of PROTOCOL.md §5 anywhere in the artifact.

## Non-blocking observation (not a finding, recorded for the author)

The new preamble enumerates the export-checkable classes as "everything under `src/`, `tests/`, `.gitignore`". Section 2 also cites `pyproject.toml:7` and `CHANGELOG.md:9` (2.11), which are likewise present in the export (both appear in the export root listing), so the enumeration is under-inclusive rather than wrong — no claim becomes uncheckable and nothing contradicts. A future editorial pass could say "everything tracked outside `collab/`, `var/`, `.git/`".

## Verdict

The exact command passes (111 passed), both round-7 findings are resolved by folds present in the true change set, the true change set contains nothing outside the fold list, and criteria 1-5 hold — the newly implicated parts on fresh evidence above, the rest standing by citation to MSG-32. No blocking finding is establishable in this pass. PASS.

Controller-Decision:
- decision: PASS

Controller-Sealed-Reveal:
- reveal-id: d02a953c0b86c614adb146c60b30178392cca1151a1128530b2486bdb5cf4b7c
- phase: sealed
- captured-at: 2026-08-20T21:49:27+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: 71240fd973150a4e7186c289484b8d2b564654cfa1449a8436dcbfd5b77eb77d
- controller-config-sha256: eb4496e756e7655abe6f8294f95458f7c259d828b28366f7a0f37dd0b9b1a2c3
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: 55e8f8dbd9195abb86f1b80f61551342ea11dfe48d65ac6a017e9cfc9b3f07df
- docket-revision-sha256: 1c5468cbf8a2fe591dc7084836c2acf71660858d4fc8ee3395e2348ccee7e2cc
- input-sha256: fb96791600101527902d2d7e6d31b0064b637079f53e12e3b893963111358241
- requested-model: claude-opus-5
- runtime-model: claude-opus-5
- reasoning-effort: high
- cli-version: claude-code 2.1.235
- isolation-mode: advisory

## MSG-37 | 2026-08-20T21:51:30+00:00 | from: codex | type: verdict | thread: plan-v080-part2-10 | refs: -

Verified the pinned docket hashes. True-diff command `diff -u var/debate/plan-v080-part2-63227/plan-round7.md docs/plans/2026-08-20-wrapper-sibling-detection-v0.8.1.md` showed only the declared round-7 folds: amendment bookkeeping, corrected source/docket location wording, and the corrected bridge-test pointer rule. Fresh source inspection confirmed `controller.py:477-478`, `578`, and `629`; the docket manifest records `collab/PROTOCOL.md` as `tracked_at_source_ref: true`. The current 3.4, I6, I10, Slice C step 5, bridge test, matrix, and E2E clauses consistently retain the documented-vendor-variable branch and the non-reserved/non-sandbox branch. Standing unimplicated checks are cited by the docket to MSG-32 (and its cited predecessors). Exact required command run from the pinned source export: `python -m pytest -q tests/test_open.py tests/test_seats.py tests/test_onboarding.py tests/test_controller.py` — result: `111 passed in 9.00s`.

Controller-Decision:
- decision: PASS

Controller-Sealed-Reveal:
- reveal-id: d02a953c0b86c614adb146c60b30178392cca1151a1128530b2486bdb5cf4b7c
- phase: sealed
- captured-at: 2026-08-20T21:51:30+00:00

Controller-Provenance:
- phase: sealed
- topology: recommended-three-agent
- author-relationship: author-independent
- profile-sha256: c0670520399fc2cbcc835908298fe8522fa607b1673e33f8696fe26bd8a57db8
- controller-config-sha256: eb4496e756e7655abe6f8294f95458f7c259d828b28366f7a0f37dd0b9b1a2c3
- source-ref: 36852335ff92d8c97db83721bfa8e99741ed1cfe
- source-manifest-sha256: bfe9fd37eb36eb384b7637629c9f1b8255920684fcee2182cd029d68bc8090f3
- docket-revision-sha256: 1c5468cbf8a2fe591dc7084836c2acf71660858d4fc8ee3395e2348ccee7e2cc
- input-sha256: 616271bbd15a50ec4c5e30447c896f10195cb728b03dc110733cc328f0701909
- requested-model: gpt-5.6-terra
- runtime-model: gpt-5.6-terra
- reasoning-effort: high
- cli-version: codex-cli 0.148.0
- isolation-mode: advisory

## MSG-38 | 2026-08-20T21:51:30+00:00 | from: owner | type: close | thread: plan-v080-part2-10 | refs: -

Controller closed the managed case as PASS. Reason: party-vote-agreement. Supervisor messages were not counted as party votes.

Controller-Terminal:
- terminal-result: PASS
- close-reason: party-vote-agreement
